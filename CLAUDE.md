# TCER — Token-to-Code Efficiency Ratio

## 项目目标

基于真实 AI 编程助手会话数据，构建多维 AI 编程效率计量体系（TCER/CTEI）。支持六个数据来源：Claude Code（`~/.claude`）、Codex（`~/.codex`）、OpenCode（`~/.local/share/opencode`）、Grok / grok build CLI（`~/.grok`）、Oh My Pi / omp（`~/.omp`）、Pi / earendil-works/pi（`~/.pi`）。

- **GUI-only**：`python -m tcer` 启动桌面界面
- **纯离线**：不依赖 git、不做联网操作，数据来自本地 JSONL / SQLite 文件
- **零依赖**：纯 Python ≥3.11 标准库

## 快速开始

``bash
python -m tcer            # 启动 GUI
python -m pytest tests/   # 运行测试
python -m tcer.audit      # 闭环审计：真实本地会话 vs 原始 JSONL 重算
``

### 闭环审计（开发必用）

改 `reader` / `analyze` / `loc` / 计价后，用真实 `~/.claude` 等数据验证，避免脑测：

``bash
python -m tcer.audit --list
python -m tcer.audit --source claude --project TCER --top 5 -v
python -m tcer.audit --source all --project TCER --json audit-out.json
python -m tcer.audit --all-projects --top 2 --no-loc   # 批跑全部项目（可加 --source）
python -m tcer.audit --all-projects --skip-empty --top 1 -q --summary-json -
python -m tcer.audit --ci --summary-json audit-summary.json   # 等价 CI 预设
# 退出码 0/1；cost_sum_per_model 校验逐模型成本加总
``

库入口：`tcer.core.audit.audit_project` / `audit_ref` / `summarize`（pytest 也可调）。检查项含：会话 Token 与文件重算一致、子代理折叠、LOC/unseen_writes、聚合 Token 求和、聚合 CTEI 与公式重算一致、Grok 无裸 `grep` 工具名、`cost_usd` 不崩、`abort_reasons` 求和 == `aborted_task_count`。`paths.project_has_sessions` 统一判断空项目（GUI 置灰 + audit `--skip-empty`）。

## 仓库结构

``
TCER/
├── tcer/                  Python 包
│   ├── core/              核心库（reader / loc / metrics / pricing / models / paths / analyze / export / format / audit …）
│   ├── gui/               GUI（app / theme / metric_defs / widgets / views / charts / popups / popups_analysis / html_report）
│   └── config/            配置（model_pricing.json / composite_baselines.json）
├── tests/                 测试（`python -m pytest tests/`）
└── doc/                   详细文档
    ├── metrics.md         指标公式与计算步骤
    ├── data-format.md     JSONL 数据格式与 LOC 原理
    ├── architecture.md    MVC 架构与工程规范
    └── style.md           GUI 界面样式规范（配色/图标/组件/图表）
``

## 指标分类（6 组 · 以 `metric_defs.GROUPS` 为准）

GUI 指标按关注维度分为 6 组（扁平，无层级关系）。数量以代码 SSOT 为准：

| 组 | 名称 | 内容 |
|---|------|------|
| G1 | 会话概况 | 元数据（时长、模型、回合、工具调用、用户消息等） |
| G2 | Token 用量 | 原始消耗（输入/输出/缓存/推理/峰值输入/窗口使用率） |
| G3 | 缓存效率 | 缓存利用率比率 |
| G4 | 代码产出与质量 | LOC、返工率、工具行为比率、搜索后编辑比等 |
| G5 | 成本分析 | 金钱代价 |
| G6 | 综合评分 | 效率指标 + CTEI 评分 + 基准参数 |

**字体颜色**：白色 = 基准值/纯数据；黄色 = 含 magic number，仅作参考。

> 指标公式、计算步骤、算例：[doc/metrics.md](doc/metrics.md)

## 指标唯一真理源（SSOT）

`tcer/gui/metric_defs.py` 是**所有指标展示的唯一真理源**——名称 / 提示 / 单位 / 格式 / 好坏方向 / 取值，4 个页签（指标分类 / 排名 / 趋势 / 模型对比）与全部 popups（雷达等）都从这里取，禁止各处再自管：

- **会话/项目级**：`raw_value(report, key)`（图表用数值）、`format_value(key, native)`（显示串）、`display(report, key)`、`report_values(report)`。每个 `Metric` 带 `fmt` 规格（`int`/`pct`/`float:N`/`money`/`text`）。
- **逐模型**：`MODEL_GROUPS` + `model_raw` / `model_display` / `model_tip`，同义指标转调 `format_value` 与会话级逐字节一致；**例外**：模型对比是 N 列并排，Token 数等大数量级用 K/M 紧凑显示（布局需要），比率/百分比仍与网格一致。
- **CTEI 因子分解**：`CTEI_FACTORS`（名称/公式）。
- **评级体系**：`core/metrics.GRADE_BANDS`（名称+阈值，best→worst）是 grade 的唯一源；`grade()`、排名分布条、趋势 CTEI 带都从它派生。
- **源能力感知**：`metric_defs._SOURCE_SUPPORT` 标注「数据源根本不产生该字段」的指标（如 Claude 无独立推理 token、Codex 无缓存写入、上下文窗口/TTFT/限流为 Codex 独有）。`display()` 返回「不适用」、`raw_value()` 返回 None，网格/图表/HTML 报告自动继承；「不适用」与「-」一样参与空单元格折叠，但颜色置灰。部分支持（如 OpenCode 多回合 peak 留 0）**不**标注，仍显示 "-"。
- **HTML 报告**：`gui/html_report.py`（无 Tk 依赖，可无头测试）渲染项目级/会话级自包含单文件 HTML，数值全部走 `metric_defs.display`，与网格逐字节一致；GUI 导出菜单含项目/会话两组入口。图表组件在 `gui/charts.py`（趋势/散点/仪表板/时段热力图，从 views 拆出，views re-export 保持旧 import 路径）。


## 工程规范

1. **禁止中间产物**：用完即删，不提交。经验写入本文件。
2. **GUI 全中文**：界面完整中文，仅 TCER 保留英文缩写。代码用缩写。
3. **纯离线**：GUI 不暴露任何需要 git 或网络的功能。**例外两处，均为用户显式 opt-in 的网络行为、未配置时零联网零入口**：①上传功能（`upload_client`/`upload_prefs`）；②LLM 语义解读（`llm_client`/`llm_prefs`，见注意事项 38）。
4. **库层不动**：`tcer/core/` 有完整测试覆盖，改动需谨慎。
5. **运行方式**：`python -m tcer`（绿色免安装）。

## 关键注意事项

1. **按 message.id 去重**：一次 API 响应被拆成多行写入 JSONL，每行重复携带 usage。必须按 id 只计一次（实测 55.9% 重复计数）。**tool_use id 去重（2.1.227+）**：rewind/重连后 Claude Code 会把历史（同 message.id + 同 tool_use id 的行及其 tool_result）重发进同一 jsonl——token 有 message.id 保护，但 tool_calls/tool_ops/LOC 回放按 tool_use id 去重（`seen_tool_uses`），否则双计。**MCP 归因键在行顶层**（2.1.207+，行内无 toolUseResult）：`attributionMcpServer/Tool` 的读取必须在 toolUseResult 分支之外。**边界**：空字符串 `""` 视为无 id，逐条计数。**ccswitch 兼容**：mimo 消息第一行是 thinking 桩（usage=0），第二行才有真实 usage；零 usage 行会释放 id 锁（`seen.discard`），允许后续行贡献真实 token。**Grok 差异**：grok build 每 turn 恰好一条 `turn_completed` 携带权威 usage，无多行重复问题，直接累加；`usage.modelCalls` 是该回合内的 API 调用次数（中位 3，`TokenUsage.api_calls`），「请求数」展示与每请求延迟均按调用数口径（ΣapiDurationMs÷ΣmodelCalls）；错误回合的空 usage（字段为 null）计入 `empty_usage_skipped`，不虚增回合数。工具名映射含 `grep_search`→Grep，并兼容实机短名 `grep`（否则探索比漏计）。**Codex 差异**：`token_count` 事件会重复投递相同的 `last_token_usage`（实测虚增 1.5–2.5%），必须对单调递增的 `total_token_usage` 做差分，`last` 仅作无 total 时的回退。**Grok user chunk**：同一条用户消息可能拆成相邻多条 `user_message_chunk`（或重复投递），相邻连续的 user chunk 只计 1 条 user_msgs，任何其他更新（`turn_completed`/`retry_state` 等）终止合并。**OpenCode peak**：session 聚合计数是多步总和，仅单回合会话可作 peak_input 近似；多回合且无 `step-finish` 快照时 peak 留 0（ratio 显示 None，勿用总和虚增窗口占用）。详见 [doc/data-format.md](doc/data-format.md)。
2. **LOC 不依赖 git、不读真实仓库**：净增代码只来自会话内工具调用回放。首次 Write 假定 old=0 并 `unseen_writes++`（F1 暴露）；Claude 的 `toolUseResult.originalFile` 到达后按会话数据回溯修正真实原行数（`note_write_original`，scan 与 session_loc_full 两路一致）。**产品边界：磁盘先验（disk_prior）、tree_loc 全树扫描、git 校准均已按「纯离线仅析会话数据」定位移除**；会话日志里记录的 git 分支/提交等元数据属会话数据，保留。Grok `write`/`search_replace` 走同一 `_LocAccumulator`。**产出文件分类**（`loc.py`）：闸门 `_is_code` = 代码(`CODE_SUFFIXES` 纯源码) ∪ 文本(`TEXT_SUFFIXES`=.md/.txt/.rtf/.rst/.org/.adoc/.tex/.csv，**策划文档也算产出**) ∪ 配置(`CONFIG_SUFFIXES`=.json/.yaml/.toml)，三类都计入 net_loc/TCER；`_is_doc_file` 把散文类文本(.md/.txt/.rst/…，**不含 .csv** 数据)单列记入「文档行」。**Office 二进制(.docx/.xlsx/.pptx)刻意排除**——AI 的 Write/Edit 是文本行模型，无法对二进制产生行增量（策划通常 .md/.txt 起草后自行转 Office）。Codex `_loc_scan` 用共享 `_is_test_file`/`_is_doc_file`（曾硬编码 .md，致 .txt 漏归文档行）。
3. **逐模型计价**：TokenUsage.per_model 按 message.model 分桶，混用多模型会话也精确。价表 `tcer/config/model_pricing.json`（≈190 模型）。**定价口径**：①录厂商**标准价**（非限时促销/介绍价，无时间相关性）；②峰谷分时录**闲时档**；③促销价/峰时价/Batch/Priority 等多轨与同模型分段计费记入条目 `_note`（`pricing.note_for()` 读取，`views._model_price_tip` 悬浮展示，让四个单价始终说明自己取的是哪一轨）。同步数据源：cc-switch `schema.rs` 为 SSOT + models.dev `api.json`（https://models.dev/api.json，更新最快、cc-switch 自身也用它审计，但双方都各有过错误记录，**价差必须厂商官方页裁决**）。**四级匹配**（`pricing._match_id`，按优先级）：①精确 ②归一化精确（小写、去 `-`/`_`、`5p2`→`5.2` 且 `5-6`→`5.6`，先于前缀以防 `glm-5p2` 误中 `glm-5`、`gpt-5-6-sol` 误中 `gpt-5`）③前缀（`claude-opus-4-8[1m]`→`claude-opus-4-8`）④反向前缀（短名 `claude-opus-4-5`→带日期 key）。每条先试原 id 再试末段 path（剥 `z-ai/`、`accounts/fireworks/models/` 等供应商前缀）。`pricing.normalize()` 把 per_model key 归一化到价表规范 key；`pricing.table_key()` 返回 None 即走 default 回退（GUI 价表浮窗据此标"默认配置价"）。匹配候选含 path 尾段与 **mode 后缀剥离**（`-thinking`/`-reasoner`，如 `claude-opus-4-5-thinking`→`claude-opus-4-5-20251101`；不剥 `-high`/`-reasoning` 以免误绑）。`pricing.unmatched_models` / `metrics.unmatched_pricing_models` 列出回退模型；状态栏与模型/成本弹窗提示。**Grok**：`turn_completed.usage.modelUsage` 同样按模型分桶（如 `grok-4.5`）；工具名优先 `x.ai/tool`，否则 `rawInput.variant` / `kind` / title（后端 WebSearch 无 tool 名时归 WebSearch）。
4. **过滤 `<synthetic>`**：ccswitch 在 429 限流或系统占位时注入伪 assistant 消息，`model` 字段为 `<synthetic>`，usage 全为零。reader 层直接过滤，不计入 `models` 和 `per_model`。
5. **子代理并入父会话**：Token 与 LOC 保留真实成本，不单独计为 session。**Grok 子代理**（新版 grok build）：子代理是同项目下的独立会话目录（`summary.json` 带 `session_kind:"subagent"`），父会话 `subagent_spawned.child_session_id` 定位——`grok_reader.subagent_dirs_of` 折叠 usage/LOC，`list_project_refs` 过滤子代理不单独计 session，`_grok_side_files_key` 并入子代理 updates 签名防缓存失效。
6. **时序分析**：`ToolOp(turn, tool, path)` 记录每个工具调用的回合序号和文件路径。**搜索后编辑比**按回合就近匹配（搜索后 3 回合内出现 Write/Edit 即算跟进，不绑定具体文件——真实 Grep/Glob 的 `path` 多为目录）；**先读后写率**等仍用 file_path。merge 时 rebase turn 编号保证聚合后时序连续。
7. **任务类型体系**：3 大类（代码创作/代码维护/非编码），每类有 TTAF 系数。`ntcer = tcer / ttaf` 归一化后可跨任务类型公平比较。`ta_tcer` 保留为向后兼容别名。
8. **返工率 = 自返工率**：churn 只计「本会话先写入、随后又被自己删除/替换」的行（`loc.SessionLoc.rework_deleted`，封顶于本会话已写入该文件的行数）；删除会话之外的既有代码属正常编辑，不计入。
9. **CTEI 三因子（聚合有效）**：`CTEI = (TCER/基准) × (CPE基准/CPE) × (1+CHR×0.5)`。历史第四因子 NCPI（净增÷代码库行数）因需扫描真实仓库已随产品定位移除，PSAC/阶段调整同删；三因子皆可聚合，**聚合层不再禁用 CTEI/评级**（audit 改为校验聚合 CTEI 与公式重算一致）。
10. **自定义 Claude 配置目录自动识别**：用户常以 `CLAUDE_CONFIG_DIR=%USERPROFILE%\.zclaude`（或其他自定义名）启动 Claude Code 以隔离 `.claude`。该环境变量只在 Claude 进程内、TCER 读不到；故 `paths.claude_config_dirs()` 以规范目录（`CLAUDE_CONFIG_DIR` 或 `~/.claude`）为锚，扫描其**父目录**里所有结构匹配 Claude 的兄弟目录（`projects/<hash>/*.jsonl` 指纹），全部视为 Claude 根。`list_projects()` 跨所有根查找，**同 hash 跨根各成一条**（每根独立成卡，`ProjectRef.config_root` 标所属根；`discover_jsonl(hash, roots=[根])` 按根分会话，`roots=None` 默认仍跨根 union 兼容 CLI）；结果按 `(home, CLAUDE_CONFIG_DIR)` 进程级缓存——会话期间新建的自定义配置目录需重启 TCER 才会出现。**Windows**：盘符大小写导致同一根内 `C--GitHub-X` 与 `c--GitHub-X` 两文件夹时，`project_hash_key` 折叠为一项（根内 casefold 并集保留），但跨根不再合并。自定义根（如 `.claude-proxy`）的项目用 ccswitch 图标（`views.project_icon_key` 按 `paths.is_custom_claude_root` 判定）。
11. **任务类型 SSOT**：`TASK_CATEGORIES` / TTAF 只来自 `config/composite_baselines.json`（`metrics._refresh_composite_globals`）。分析入口默认 `code_creation`；`resolve_task_type` 把空值/未知/`feature` 等合法化，`coerce_task_type` 给公式层（未知→None，不静默套创作系数）。`task_type=auto`（GUI「自动」）按会话 `infer_task_type`（net_loc/探索比/Edit 比/读写比）推断，聚合取众数。个人基准默认至少 `MIN_BASELINE_SESSIONS=10` 条完整会话。**`ctei()`/`compute()` 的 `tcer_baseline`/`cpe_baseline` 参数默认 `None`→调用时读实时全局**（不可用 `=TCER_BASELINE` 默认值：默认参数在 def 时冻结，`save_baselines` 重绑全局后会用陈旧基准，CTEI 直到重启才更新）。
12. **Claude 单次扫描**：`reader.scan_session` 一趟 JSONL 同时产出 TokenUsage + SessionLoc；`analyze` 进程内按 path 缓存，避免 usage 与 LOC 双读。GUI `reanalyze` 用 `cancel_event` 协作取消上一次分析。
13. **high_churn 合并**：子代理折叠/项目聚合用 `loc.merge_session_locs`，按合并后的 `file_edit_counts` 重算 `high_churn_files`（同路径不重复计）。
14. **mtime 缓存**：`tcer.core.file_cache` 按 `(path, mtime_ns, size, variant)` 缓存 scan/usage/meta（LRU，上限 2048——512 时「全部项目总览」工作集（44 项目 ≈675 条）发生轮换，热态总览退化到近冷态 4.3s；2048 后 0.25s。claude `read_session_meta` 与 omp 同样入缓存，返回共享引用勿改）。取消靠 `cancel_check` 在 factory 内抛异常实现，部分扫描天然不入缓存——因此 GUI 的可取消分析**同样走缓存**（勿再用 `cancel_check is None` 做缓存开关）。四源均已接入：Claude `scan_session`、Codex/Grok usage+loc、OpenCode usage+loc（SQLite 会话 key 到 db 文件，粗粒度；legacy 会话 key 到自身 JSON）。Claude 日期过滤复用 cwd-keyed 扫描，同文件不再双扫。测试可用 `file_cache.clear()`。
15. **用户消息懒加载**：分析只计 `user_msgs` 数量；Claude 与 Codex 一样弹窗/上传时再 `read_user_messages`。**弹窗不并入子代理文件**：子代理 jsonl 里的 user 消息全是 Task 工具的派发 prompt（「You are researching…」之类），非真人输入，并入会混出假「用户消息」——`app._claude_user_messages` 只读主会话（上传 `upload_client` 仍含子代理，归上传负责人）。
16. **缓存写 TTL 分档计价**：Claude `usage.cache_creation.ephemeral_1h_input_tokens` 是 1h 缓存写子集（单价 2×input，5m 为 1.25×input）。`TokenUsage.cache_write_1h_tokens` 记录该子集，`metrics._cost_from` 按 `CACHE_1H_PREMIUM=0.6` 加溢价（价表 cache_write 视为 5m 率）。实测本机 60% 缓存写走 1h 档——漏掉会系统性低估成本。
17. **深度信号解析**：Claude `type:"system"` 子类型：`turn_duration.durationMs`（**整轮墙钟**耗时→回填 `turn_stats`；含该轮多次 API 补全 + 工具执行 + 用户等待，`messageCount` 可达 400+，**非单次生成耗时**，故不用于 `output_tps` 输出吞吐——Claude 该指标显示「不适用」）、`api_error`（429→限流命中）、`compact_boundary`（压缩）；`usage.server_tool_use`（网页搜索/抓取）；行级 `version/gitBranch/effort/permissionMode` → SessionMeta。Grok 每会话另读 `signals.json`（窗口/TTFT/ITL/取消/评价/git 落地/回退行/上下文压缩数 `compactionCount`→`compaction_count`；`contextTokensUsed` **覆盖** peak_input——turn_completed.usage 是回合内多补全总和，按回合累计的 peak 虚高 10×+）与 `events.jsonl`（`permission_resolved.wait_ms` 审批等待）。改支持范围后同步 `metric_defs._SOURCE_SUPPORT`。
18. **逐回合时间线**：`TokenUsage.turn_stats: list[TurnStat]`（turn/ts/4 token/duration_ms/tool_calls/errors），四源填充：Claude 逐响应、Codex 逐 token_count 差分步（task_complete 回填耗时）、Grok 逐 turn_completed、OpenCode 逐 step-finish。merge 时 rebase turn（聚合对象时间线是串接非并发）。GUI「会话时间线」弹窗与会话级 HTML 报告消费。**输出吞吐 `output_tps`**（G2，字/秒）= Σ output ÷ Σ 单次补全 `duration_ms`（秒），仅 `{codex,grok,omp}` 支持（有逐补全生成耗时，业界标准 output÷解码时间）；Claude 只有轮级墙钟不可用、OpenCode/Pi 无逐补全耗时，均显示「不适用」（`_SOURCE_SUPPORT`）。
19. **F1 修正（originalFile）**：Claude 工具结果行 `toolUseResult.originalFile` 携带 Write 前真实原文。`_LocAccumulator.pending_f1` 记录按 old=0 记账的首个 Write，`note_write_original` 回溯修正（覆写→按 new−orig 重算并撤销 unseen；确认新文件→只撤销 unseen）。`scan_session` 与 `session_loc_full` 两条路径都做同一修正（审计交叉验证要求逐字节一致）。`toolUseResult.userModified` → 人工修正计数（采纳信号）；OpenCode `session.revert` / Grok `hasReverted` → `revert_events`。
20. **Codex 工具错误识别**：exit code 有两种实测格式（`Process exited with code N` / `Exit code: N`），exit code 权威；无码时只认显式失败前缀（`execution error`/`error:`/`failed:`/traceback），禁止全文 `error` 子串匹配（误报）。
21. **SourceAdapter**：非 Claude 五源（Codex/OpenCode/Grok/omp/pi）共用 `analyze._analyze_source_project` 骨架 + `_SourceAdapter` 钩子（resolve/sessions/read_meta/usage_of/loc_of/session_key），file_cache key 构造在钩子内（Grok 必须并入 signals.json/events.jsonl 旁路文件签名，否则会话结束后补写的信号不失效）。新增数据源 = 写一个 adapter。共享小工具在 `core/parse_util`（as_int/first_str）。
22. **护栏测试**：`tests/test_models_merge.py` 反射断言 `TokenUsage.merge` 覆盖全部字段（新增字段忘改 merge 会当场失败，容器/极值字段进 `_SPECIAL` 表）；`tests/test_gui_smoke.py` 无头构建全部图表模式与弹窗（无显示环境自动 skip）；`tests/test_export.py` 断言 `report_row_dict` 每个键必须归入 `_CSV_FIELDS` 或 `_CSV_EXCLUDED`。
23. **模型对比分摊**：`compare_models` 行为/产出/质量指标按 token 占比加权（`_weight_sum`），单模型会话权重 1.0 与旧「主模型全额归因」一致，混合会话按占比拆分不再丢弃。
24. **GUI 样式**（完整规范见 [doc/style.md](doc/style.md)；配色/间距/字体 SSOT 在 `tcer/gui/theme.py`）：①Windows 高 DPI 感知在 `app._enable_windows_hidpi`（**必须在创建 Tk 之前**调用，否则整窗位图拉伸发糊）+ `_apply_tk_scaling`（mac/Aqua 跳过——Tk 自管点单位，显式改会虚胖）；恢复的窗口几何经 `app.clamp_geometry` 钳进当前屏幕（1920×1080 存的窗口搬到小屏/mac 会超屏，多显示器拔掉后 +3000 偏移会让窗口整个落在屏外），默认尺寸随屏幕收缩；②按钮一律用 `widgets.flat_button`（`primary=True`=强调色主操作），禁手写 `tk.Button(relief="flat")`（UploadDialog / 删除确认弹窗例外）；③间距从 `theme.PAD_XS/S/M/L`（2/4/8/12）取值；④**禁用 emoji 当图标**——统一 Icons8 material-outlined 白线 16×16（存 `gui/assets/ui-*.png`，入口 `views.ui_icon`），来源/厂商品牌图标不动；身份图标标准：项目→`ui-project`、会话→`ui-session`（双气泡）；⑤**弹出菜单必须用 `widgets.FlatMenu`**（Windows 原生 `tk.Menu` 有去不掉的白边系统 chrome，`borderwidth=0` 无效）；⑥**聚焦环全局消除**（tk `highlightthickness=0` / ttk 移除 focus 元素 + `focusthickness=0`）。**Windows 经典 `tk.Button` 的持焦蚂蚁线是系统 `DrawFocusRect` 画的，`highlightthickness=0` 挡不住**（像素差分实测），唯一解法是 `takefocus=0` 不抢焦点——`flat_button` Windows 分支两者都传（曾只传前者，用户实测虚线框仍在；mac 分支是 Label 无此问题）。例外：删除确认弹窗**刻意** `focus_set()`（回车不误删），保留焦点行为；⑦Notebook/Combobox 深色化陷阱：`style.map` 优先级高于 `configure`，active 态白边与页签虚线聚焦框都得在 map / layout 里压（下拉列表另经 `root.option_add`）；⑧**图表折线/点抗锯齿**走 `charts._aa_layer`（PIL 2× 超采样贴回 canvas；Tk RGBA 照片转换成本随 alpha 种类数暴涨——LANCZOS 边缘上千种渐变 alpha 曾让一次重绘 107ms/层，`_aa_layer` 内量化到 16 档后 ~23ms、视觉不可辨，勿删），**数值禁科学计数法**走 `_fmt_num`（大数 K/M/B，与模型对比页一致；指标语义值仍走 metric_defs `format_plot`）；⑨仪表盘折线用分类色**提亮版** `_DASHBOARD_LINE_COLORS`（原 `GROUP_COLORS` 偏暗、画细线灰蒙蒙，提亮后同色相按色识组），标题去 G1–G6 前缀；⑩趋势图例标签底色随所在头部容器、色点用 10×10 实色块；⑪`ScrollFrame` 自带按需显示的深色滚动条；⑫界面偏好（几何/分栏/筛选/上次项目/启动检查开关）经 `core/ui_prefs` 持久化到 `core/app_dirs.prefs_dir()`（发布版 exe 同目录，回退 `~/.tcer/`；早期 `~/.claude/` 自动迁移），与 Claude 数据源 `~/.claude` 分离；⑬**Toplevel 子窗口不设显式「关闭」按钮**（标题栏 × + Esc 即可,显式关闭按钮冗余;`UpdatePopup` / `RadarPopup` / `ConfirmDeletePopup` / `UploadDialog` 均此模式）。色值表 / 各组件 why / 图表细节见 [doc/style.md](doc/style.md)。
25. **排名页**：CTEI 三因子化后默认即可计算，排名页默认就有数据；仅当会话无 CTEI（no_loc 或无净增行/成本）时回退按 TCER 排名并显示提示条。`launch.bat` 优先 `pyw`/`pythonw`（`start` 分离，无残留控制台；文件为 GBK 编码保 cmd 中文注释）。
26. **omp（Oh My Pi）数据格式**：会话存于 `~/.omp/agent/sessions/<dir-encoded>/<ts>_<sessionId>.jsonl`（`PI_CODING_AGENT_DIR` 重定位 agent 基目录、`PI_CONFIG_DIR` 重定位 `~/.omp` 根；`omp_sessions_dir` 解析同 omp `dirs.ts`，未复刻 Linux XDG 重定向）。JSONL 每行一个 `SessionEntry`：首行 `type:"title"`（定宽标题槽，跳过）、`type:"session"` 头（`id`/`cwd`/`title`/`timestamp` 为 **ISO-8601 字符串**，`parse_timestamp_ms` 兼容）、`type:"model_change"`（`model` 为 `"provider/modelId"`，`pricing.normalize` 经 `rsplit("/")` 剥供应商前缀）、`type:"thinking_level_change"`（`thinkingLevel` 如 `high` → `reasoning_effort`，末个非空值胜出，pi 同源继承）；assistant 的 `stopReason=="aborted"`（用户中断/运行中止）计入 `aborted_task_count`、`errorMessage` 作 `abort_reasons` 键（语义同 Codex `turn_aborted`，pi 共享同一 `_aggregate_single`），`aborted_tasks` 指标 `_SOURCE_SUPPORT` 含 `{codex,omp,pi}`、`type:"message"`（`message.role` ∈ `user`/`assistant`/`toolResult`）。assistant 携带**每响应一个** `usage={input,output,cacheRead,cacheWrite,totalTokens,cost:{total}}`（无 Claude 多行重复，直接累加）+ `contextSnapshot.promptTokens`（peak_input）+ `duration`/`ttft`（回填 turn_stats / time_to_first_token_ms + `ttft_ms_samples`）；content 块 `thinking`/`text`/`toolCall={id,name,arguments}`；user content 的 `{type:"image",mimeType,data}` 内联图片计入 `image_count`（图片输入，`_SOURCE_SUPPORT` 含 `{codex,opencode,omp,pi}`）；`toolResult={toolCallId,toolName,content,details,isError}`。LOC：`write` 取 `arguments.content`+`details.resolvedPath`（unseen_writes 同 Grok）；`edit`/`ast_edit` 取 `details.{oldText,newText,path}` 经同一 `_LocAccumulator`（净增行差 + 自返工）；`snapshotsPruned`（无 oldText/newText）的 edit 跳过。`custom`/`custom_message` 等非 message 行忽略。**子代理折叠**：omp 子代理会话存于主文件同名的 `<stem>/` 子目录（`..._<uuid>.jsonl` → `..._<uuid>/SubAgent.jsonl`），`_is_subagent_file` 按「父目录名 == 某 main 文件 stem」识别，`sessions_for_project`/`session_paths` 只返回 main，`aggregate_usage`/`_loc_scan`/`read_user_messages` 把子代理合并入父（真实成本保留，不单独计 session），`_SourceAdapter.subagents_of` 钩子统计折叠数（`n_subagents`），file_cache key 并入子代理文件签名。env 优先级：`PI_CODING_AGENT_DIR` > `PI_CONFIG_DIR` > `OMP_HOME`（legacy）> 默认 `~/.omp`。能力映射见 `metric_defs._SOURCE_SUPPORT`（omp 与 Claude 同为 Anthropic 语义：cache 读写分列、无独立 reasoning；另支持 ttft/ttft_p95/web_searches，不支持 context_window/compactions/rate_limit）。`read_session_meta` 走 file_cache（要全文件扫 title 变更/首条用户消息，30 会话项目重复分析曾花 150ms，缓存后近零）。 **prompt 行为信号**（斜杠命令 `slash_command_count`、纠正措辞 `correction_msg_count`、首条消息长度 `first_prompt_chars`）与 Claude 同构（user 文本消息），在 `_aggregate_single` 的 user 分支采集；判定逻辑（`is_slash_command`/`is_correction`/`CORRECTION_RE`）移至 `parse_util` 作跨源 SSOT（Claude/omp/pi 共用一张正则避免漂移）；子代理的 user 消息是 Task 派发 prompt，`_aggregate_single(is_subagent=True)` 跳过这三个信号（同 Claude 的 `is_sub` 守卫），成本计数仍保留。`_SOURCE_SUPPORT` 三键含 `{claude,omp,pi}`。
27. **Codex 会话发现（resume 去重 + 头部快读）**：Codex `resume` 会**复用同一 `session_id` 写新 rollout 文件并重放整段历史**（`rollout-<新时间戳>-<同一uuid>.jsonl`），一个逻辑会话 = 多个累积快照文件（最新为超集）。`codex_reader.list_project_refs` 必须按 `session_id` 折叠：`_dedupe_by_session_id` 每组只留 `mtime+size` 最大的文件（最完整快照）。**只去重不合并求和**——累积快照求和会重复计 token（违背 #1 铁律）；`session_index.jsonl` 实测 stale（7 条 vs 磁盘 15 唯一 id），不可作去重依据。**性能**：项目列表只需 `cwd`+`session_id`（都在第 1 行 `session_meta` 头），`_session_head_meta` 只读头不扫整文件（实测 24×：整文件 `read_session_meta` 5.7ms/文件 → 头读 0.2ms/文件），重 Codex 用户启动从扫全部字节降到 O(文件数)。头读不进 file_cache（已够快且避免挤占共享 512 LRU）。全路径（GUI/CLI/audit/upload）经 `session_paths` 单点生效。
28. **`tool_variants` 补录技能身份**：`tool_calls` 按工具名计数，但 `Skill`/`Task`/`Agent` 三者的工具名不含被调用的技能/子代理，`reader.record_tool_variant` 从入参补录 `"Skill:dataviz"` / `"Agent:Explore"` → 次数（`_VARIANT_KEYS` 表驱动）。**仅 Claude 源**（其余 CLI 未以已解析的形状暴露该身份）；MCP 服务器不需要它——`mcp__<server>__<tool>` 键本身就带服务器名，五源通用。server 端据此构建 Skill / 子代理 / MCP 三个对比维度。
29. **server 聚合口径与桌面端一致**：`server/backend/db._agg_metrics` 的比率一律「分子和 ÷ 分母和」重算（返工率按 `code_added` 加权、工具错误率按工具调用数加权），**不取算术平均**（否则 5k 与 2M token 的会话等权，触发 Simpson 悖论）；无分母的比率（先读后写率/搜索后编辑比，分母来自工具时序、库里没有）退化为中位数并由 `_stat` 标注。**聚合 CTEI 按规则 9 重算**（`ctei(聚合TCER, 聚合CPE, 聚合CHR)`，与 audit 的 `aggregate_ctei_recompute` 同一口径），不对各会话 CTEI 取平均。决策实验室（`server/backend/analysis.py`）在此之上做「任务类型 × 会话规模」分层 + bootstrap 区间 + 证据分级，区间跨 0 一律不排名。
30. **检查更新 / 直接更新 / 配置存储**（opt-in 联网 + 绿色便携）：①**配置存储** `core/app_dirs.prefs_dir()` 统一 TCER 自有配置（`tcer_ui.json` / `tcer_upload.json`），发布版（`sys.frozen`）优先 **exe 同目录**（便携，删 exe 即净）、不可写回退 `~/.tcer/`、源码运行用 `~/.tcer/`，**不再用 `~/.claude/`**（那是 Claude Code 的目录），旧位置 `~/.claude/tcer_*.json` 首次启动自动迁移；②**检查更新** `core/update_check` 纯标准库查 GitHub Release latest（失败静默返回 `None`）+ `render_notes` 清理 markdown 供 Tk 展示（去 `##`/反引号/粗体，列表转 `•`），opt-in 联网仅查公开 Release、**不发送用户数据**（区别于上传 #3），启动检查默认值随运行形态（发布版 `frozen` 默认开、源码默认关），用户在「工具」菜单切（存 `ui_prefs['check_update_on_start']`）；③**直接更新** `core/updater`（仅发布版 `frozen`）：`asset_for_current_platform` 选包 + `download` 流式下载（进度回调）+ `apply_and_restart` 平台替换（Windows 生成 `updater.bat` 轮询删旧 exe→move 新→重启→自删，`mbcs` 编码兼容中文路径、绕过运行中 exe 锁；mac 覆盖二进制 + `xattr` 清 quarantine + 重启），**调用后须立即 `SystemExit`**；`UpdatePopup` 发布版且有平台包时显示「立即更新」（`controller.start_self_update` 后台下载，进度经 `root.after` 回主线程），否则回退「前往下载」。
31. **Pi 数据格式（earendil-works/pi 上游）**：Pi 是 omp（#26）的**上游原生**（omp 是其 fork，同族 JSONL 但有实质差异）。会话存于 `~/.pi/agent/sessions/<dir-encoded>/<ts>_<sessionId>.jsonl`（**仅** `PI_CODING_AGENT_DIR` 重定位 agent 基目录；**无 `PI_CONFIG_DIR`**，默认根固定 `~/.pi`，`paths.pi_sessions_dir`）。与 omp 的差异：①**无定宽 `type:"title"` 标题槽**，首行直接是 `type:"session"` 头（`_session_line` 照常定位）；②assistant **无** omp-fork 加的 `contextSnapshot`/`duration`/`ttft` → `peak_input` 留 0、TTFT 不可用；③usage 多两字段 `cacheWrite1h`（1h 缓存写子集）+ `reasoning`（推理 token）。实现：`pi_reader` 是**薄壳**，逐函数复用 `omp_reader` 解析层（同 schema），仅 override `discover_sessions`/`list_project_refs`/`resolve_project`/`sessions_for_project`/`read_session_meta`（source/entrypoint="pi"）。两增强字段由共享的 `omp_reader._add_turn_usage` 读取（**向后兼容**：omp 数据无此二字段读 0，行为不变；故 omp 测试不回归）：`cacheWrite1h` → `cache_write_1h_tokens` + bucket 第 5 参（1h 溢价计价见 #16）、`reasoning` → `reasoning_output_tokens`。**reasoning/output 归并**：按 models 约定 `output` 已含 reasoning（同 Codex），reader 只填比率字段不加进 output；若 audit 实测 `input+cacheRead+cacheWrite+output ≠ totalTokens` 则 output 不含 reasoning，需并入 `output_tokens` 补成本（待真实 `~/.pi` 数据闭环）。工具映射 `_PI_TOOL_MAP = _OMP_TOOL_MAP`（同源子集，未知名 pass-through）；子代理折叠复用 `_is_subagent_file`（布局相同则折叠，否则当主会话不崩）。能力映射（`metric_defs._SOURCE_SUPPORT`）：pi 支持 reasoning/reasoning_ratio/web_searches/cache_write/cache_write_ratio/cache_efficiency，**不支持** ttft/ttft_p95（无 ttft 字段）及 context_window/rate_limit 等。GUI 显示名「Pi」，与 omp 并存（不同目录/用户群）。
32. **LOC 按结果裁决（is_error 不回放）**（已知边界：Edit 的 replace_all 多处替换只按一次匹配计行差，LOC 系统性少计——无文件内容无法数出现次数，由「LOC 可信度」指标暴露偏差）：编辑类 tool_use（Write/Edit/MultiEdit/NotebookEdit）在 `scan_session`/`session_loc_full` 中先按 `tool_use_id` 缓冲（`pending_edits`），等对应 tool_result 到达且**非 `is_error`** 才回放——失败/被用户拒绝的调用不产生行增量、不污染 `file_lines` 基线、不计 `unseen_writes`。结果未到的（截断/进行中会话）在文件末尾按成功回放（宁可多计不可漏计）。连带顺序约束：`toolUseResult.originalFile` 的 F1 修正必须**延迟一行**处理（Write 记账现在发生在结果行，先记账再修正，否则 `pending_f1` 未建立修正丢失）。`on_tool_use(name, inp, turn=None)` 可选回合号 → 逐回合 LOC 流水 `TokenUsage.turn_net_locs`（分段 TCER 用；session_loc_full 无回合概念不产出，SessionLoc 两条路径仍逐字节一致）。
33. **Grok 字段位置与失败口径**：①`modelId` 实测全在 `params.update._meta`（`_meta_of` 读的 `params._meta` 为 0/446），reader 两处都查、`update._meta` 优先；②工具失败判定 = `exit_code` 非零 **或** `status=="failed"`（SearchReplace NoMatchesFound / TaskOutput TaskNotFound 等无 exit code 的失败与 Claude `is_error` 同义），二者并存只计一次。
34. **新指标族（v1.6.0 审查批次）**：⓪**压缩代价** `TokenUsage.compaction_discarded_tokens/pre_tokens_max/duration_ms_total`（Claude compactMetadata；触发方式记 abort_reasons `compact:auto/manual`）；**自报成本偏差** `cost_reported_ratio`（价表计价÷源自报，opencode/omp/pi）；**LOC 可信度** `loc_patch_agreement`（回放 vs structuredPatch，仅 Claude）；**限流洞察**（429 ≥3 次触发 drag）。Claude `usage.output_tokens_details.thinking_tokens` → reasoning（已含在 output，只作占比）；`server_tool_use` 块与 user 内联 image 块计入 tool_calls/image_count；`plan_mode_reentry` 计入计划模式；omp `mode_change(plan)`→计划模式、`stopReason=error`+Too many requests→限流命中、零 usage 响应 duration 不落上一 TurnStat；grok 审批次数只计 wait_ms>0 的真实人审。①**重试循环** `metrics.retry_loop_metrics`（同工具+同路径连续 ≥3 次；`retry_loops`/`retry_loop_max` 指标 + insights 卡死规则；无路径工具不参与）；②**单回合成本** `turn_cost_analysis`（最贵回合占比 `turn_cost_share` ≥30% 触发 insights「大户回合」；`cache_invalidations` 缓存失效事件 = cw 翻倍且 cr 回落——**已按用户要求从指标面板移除展示**，计算与 CSV 字段保留）；③**人机时间** `activity_metrics`（`ai_active_ratio` = Σ回合耗时÷墙钟，Grok 扣审批等待，Claude 为上界估计；`user_gap_median` 相邻回合间隔中位数，只统计 1–30 分钟）；④**分段效率** `segment_metrics`（回合三等分算分段 TCER，`efficiency_decay` 末/首比 <0.5 触发「上下文拖累」insights；`TokenUsage.compaction_turns` 记压缩位置，merge 随 turn rebase）。⑤**项目级分析** `analyze.file_hotspots`（跨会话热点文件）/ `model_switch_stats`（委派 vs 升级救援，按价表 output 价排方向）/ `tool_profile`（技能/子代理/MCP 复用）——GUI「项目画像」弹窗消费。
35. **GUI 新视图（v1.6.0）**：趋势页新「相关」pill = `CorrMatrixChart`（N×N Pearson r，正=ACCENT 深浅/负=WARNING 深浅，点格下钻散点图自动设轴）；仪表盘右键格子换指标（`ui_prefs['dashboard_metrics']` 持久化，重启恢复）；会话卡片摘要行（左主模型短名、右成本金额纯文本；无评级徽标）；SessionColumn 卡片只建一次、搜索/红旗过滤走 pack 复用（~600ms→~1ms），构建分批 after(1)（勿用 after_idle——update_idletasks 会把 idle 批全部同步跑完）。切项目走页签惰性渲染（`app._refresh_views/_render_tab`：只画当前页签，其余标脏、`<<NotebookTabChanged>>` 补渲染）。`SelectableLabel` 自适应高度合并结算（类级待测队列 + 单定时器；tkinter 的 after 回调注册在调度 widget 名下、widget 销毁则定时器失效——须锚定 toplevel 并自愈，见 widgets.py 注释）——逐实例 update_idletasks 曾让效率榜一次刷新卡 2s；时间线弹窗点击回合展开明细 + CHR 虚线/累计净增行/压缩竖线叠加，另含「收敛诊断」双视图（左轴累计净增 × 右轴累计成本剪刀差 + 重试循环横带/最贵回合▼/缓存失效×标记；数据由弹窗调 `metrics.turn_cost_analysis`/`retry_loop_metrics` 现算——两函数返回逐回合序列 `turn_costs`/`cache_invalidation_turns`/`spans`，`compute()` 挂载点与 `SessionReport` 不挂序列，避免 CSV/HTML 护栏连带；非 Claude 源无 `turn_net_locs` 时净增线降级不画）；会话对比顶部叠加雷达 + 逐回合曲线；项目总览气泡散点（X=成本 Y=TCER 气泡=Token，点击选表行）；HTML 项目报告选节导出（`render_project_html(sections=...)`，`HtmlSectionsPopup`）。
36. **评分门控与评级带校准（v1.7.1）**：①零/负净产（net_loc≤0）会话**不评分**——`metrics.score_axes` 直接返回三轴 None（实测 65% 会话零净产，证据收缩把它们全钉在恰好 50 分的「中等」，常数分伪装信息；排名页/insights 对 None 已有兜底）；②`score_tiers.bands` 按 133 个真实已评分会话分位重划（优秀>68≈P90 · 良好≥57≈P70 · 中等≥47≈P20 · 待改进≥42 · 低效<42，旧 75/60/45/25 把 87% 压进中等），`metric_defs._tier_band_hint` 从 `SCORE_TIER_BANDS` 动态生成提示防漂移。（曾实现过「综合分百分位」逐会话指标，按用户要求移除——无意义。）**harness 影响诊断结论（跨源比分的四通道）**：恒等式 `CPE×TCER=1000×p̄`（p̄=混合单价）⇒ 产出轴×成本轴实测相关 0.63、80% 权重压在 TCER 一个潜变量上；跨源差距来自 口径/token 构成（p̄ 0.37~1.14）、行为（工具/百行 16.9~46.5）、任务组合（grok 75% non_coding 且净产中位 2961 行）三层；同模型同源名下还叠「部署路由」层（代理会话不上报 cache 字段）。**③逐源基准（v1.7.1）**：`baselines_per_source` 配置块 + `resolve_baselines(uid, source=)` 解析链（逐项目 > 逐源 > 全局）+ `save_baselines(source=)` + `compute_baselines_per_source`（每源独立过 MIN_BASELINE_SESSIONS 门槛）；analyze 两条路径的基准回退与 GUI reanalyze 走同一链；GUI「计算个人基准」弹窗加「按数据源」模式。不写入则维持全局基准=系统分口径（保留 harness 真实差异）。**④同模型跨源对照（v1.7.1）**：`analyze.cross_source_models(reports)`（fixed model vary harness：`_dominant_model_key` 按 token 量取主模型经 `pricing.normalize` 归一 × source 分桶，≥2 源且每源 ≥`CROSS_SOURCE_MIN_CELL`=3 个**有产出（net_loc>0，与评分门控同口径——零净产 tcer=0.0 非 None 不过滤会把中位数拖到 0）**的会话才成组，数值为会话级中位数）→ GUI「工具 → 同模型跨源对照」`CrossSourceModelsPopup`（两级树 + 窗底归因口诀：TCER/工具每百行差=行为、p̄/CPE 差=价格组合、净产差=任务组合）。**待做**：成本轴改 p̄ 比率与 TCER 解耦（正交化，需指标版本化 + audit 重验）。
37. **真实项目聚合页签（v1.7.1，「项目聚合」）**：同一工作目录混用多个 agent 是真实形态（本机实测 42 卡 → 22 个真实项目，主力项目 4–5 卡：claude×2 配置根 + codex/grok/omp）。**刻意不改主界面项目列表**——只加独立页签：①库层 `analyze.RealProject` + `real_projects(refs)`：聚合键 = `normcase+normpath(cwd)`（盘符大小写/斜杠/尾斜杠折叠），**父子路径不合并**（`c:\playground` ≠ `c:\playground\langfuse`）；Claude ref 层无 cwd → `_claude_ref_cwd` 读首会话 meta（`read_session_meta` 走 file_cache；**key 反解不可靠**——Claude 编码把 `.`/空格折叠成 `-` 有损）；读不到 cwd 的 ref 独立成组（`ref:<source>:<key>`），宁可不合并不错合并。**不同盘符同名工程不做自动聚合**（basename 判据误伤率高、纯离线不能读 git remote 验证；将来可做手动别名）。②GUI `views.RealProjectsView`（第 5 页签，**卡片式三行布局**）：第 1 行项目路径（`_display_cwd` 盘符统一大写——元数据常见小写 `c:\`）+ 右侧成本金额（**与会话卡片同款**：`$` 两位小数、方向着色 >0 红/$0 绿、`FONT_VALUE`；卡片不显示评级徽标，同会话卡约定）；第 2 行 agent 品牌图标条（`source_icon`，与项目卡同款图标库，Claude 自定义根自动用 ccswitch 图标——图标条本身区分两套部署；悬停见来源名，**图标优先于文字标注**）；第 3 行摘要「N 会话  N 请求  N 用户消息  X亿 Token」（空格分隔；请求数口径同 SSOT——Grok 按 API 调用数、其余按助手响应数，悬停有完整说明），净增/TCER/千行代码成本/缓存命中率放展开明细不在卡面堆砌，Token 大数用 `fmt_approx_cn` 中文量级「21.6亿」。点卡展开各源明细 = **固定列网格**（来源 + 按语义分组排列：总会话/总请求数/总用户消息 → 总 Token/总成本/平均千行代码成本 → 总净增行/平均TCER → 平均缓存命中率 → 项目综合效率分（聚合级，`f"项目{metric_name('score')}"`——与效率榜项目视角同名同派生）；**列名口径前缀（用户约定，优先于「SSOT 全名」守则）**：求和列冠「总」、比率列冠「平均」（实为按合计重算 ≠ 会话算术平均——Simpson 悖论；SSOT 全名与公式在表头悬浮）；表头 + 定宽列 grid 跨行严格对齐，来源列 weight=1 拉伸、数值列右对齐、**除 TCER 外不用缩写**）。**默认按会话数排序**，排序走工具栏 FlatMenu 下拉，同源多卡在标签标注所属根（如 `Claude（.zclaude）`）。③app `real_projects_scan`：后台扫全部项目，**聚合口径比率一律「分子和÷分母和」重算**（TCER/CPE/CHR/返工/错误率按合计，不取各卡平均——Simpson 悖论），效率分从聚合轴输入重算（聚合 TCER 作产出轴代理、先读后写取中位数，同 server `_agg_metrics` 处理）。页签**不接入 `_refresh_views` 脏标记循环**（不依赖当前分析，首次切入自动扫、工具栏刷新强制重扫）；`_render_tab` 对 idx==4 特例先于 `_current` 检查（未选项目也能看）。

38. **LLM 语义解读层（opt-in 第二处联网例外）**：给会话时间线弹窗加 LLM 过程分析。三条铁律：①**未配置零联网**（工具栏「LLM设置」与时间线「LLM 解读」按钮**均常驻**——点击只开本地设置弹窗或提示引导，网络调用只发生在用户确认之后）；②**结果只进解读层**——LLM 输出仅弹窗叙事展示（脚注标注 model/scope/时间/「不参与指标计算」），永不进 CTEI/评分/CSV，保 audit 闭环的确定性；③**零依赖**——纯 urllib 调 OpenAI-compatible `{base_url}/v1/chat/completions`（非流式），`normalize_base_url` 自动补 `/v1`（主流兼容服务全挂 /v1 下），本地 Ollama 一等公民（空 api_key 不发 Authorization——部分代理对空 Bearer 400）。**模块**：`core/llm_prefs`（独立 `tcer_llm.json` 原子写；多选复选 scopes SSOT（默认全部勾选：metrics=会话指标与时序 / dialog=对话交互时间线 / tools=工具调用与代码变更；向下兼容单选 full/dialog/metrics。按用户诉求放开代码出境约束：dialog 完整呈现用户指令与 AI 回答全文，取消字符截断与 6 万预算掐腰限制；tools 直接提取 Edit 原代码与新代码 diff 差异、Write 正文及 Bash 命令，为大模型过程分析提供代码级实证支持）、`core/llm_client`（`_post_chat` 复刻 upload_client 错误三分支）、`core/llm_prompts`。**解读质量的关键设计（v2，用户实测 v1「与话题关联性很低」打回后重做；v3 审计化重做，2026-09 用户四点反馈：①LLM 说好话概率高、看不出是 AI 问题还是用户反馈序列问题；②术语（气态/水床）非技术本科生看不懂；③1M 配置只喂 5-6 万 token 太少；④转折反转细节不够）**：
- **反谄媚审计立场（system prompt 最高优先级段）**：角色从「教练」改「审计员」——禁笼统正面评价、每条判断绑定回合号+原文证据、确无问题须声明「经核查未发现」并列证据点（不许编造也不许回避）、结论从严不从宽、**转折归因必须明确**（AI 理解偏差/自作主张 vs 用户指令模糊/纠正太晚 vs 环境报错，混合责任说清份额，禁止「双方都有责任」和稀泥）。convergence 输出六节结构以「关键转折逐一深挖（位置/转折前/触发者/转折后/责任判定，篇幅占一半）」+「反馈序列审计（逐条干预效果 + AI x%/用户 y%/环境 z% 责任占比）」为核心；dynamics 遥测 trajectory 节点新增 `trigger` 字段（user/ai/env/none，与正文归因一致），HUD/Tooltip 显示「本轮驱动：用户消息推动/AI 自主决策/报错驱动」。
- **术语平实化**：正文（system prompt 硬约束）与 GUI 文案（views 徽标/HUD/图例）全面禁物理黑话，替换表：气/液/玻璃/晶态→探索期/构建期/卡壳期/收尾期、水床效应→按下葫芦浮起瓢、势垒→关键转折关口、狄拉克目标→目标点/用户真正想要的结果、平庸吸引子→惯性套路区、卡诺效率→代码有效率、收敛→回到正题/接近目标。遥测 JSON 英文枚举是机器协议保留不动。读者画像钉死为「非计算机专业大学本科生」。
- **供给档（`llm_prefs.dialog_detail`：standard/rich/full，默认 full，按用户 1M 配置定）**：`read_dialogue(path, detail=)` 的关键扩容——**正常工具结果**（Grep/Glob/Read/Bash 输出，AI 的观察面）standard 档丢弃（历史行为）、rich 档纳入摘要（探索/执行类 400 字符、杂项 200）、full 档加倍（800/400，配 1M 窗口）；tool_result 按 tool_use_id 去重防 rewind 双计，tool_use id→工具名映射定制摘要长度。没有观察面模型只看得到「AI 做了什么」看不到「它当时看到了什么」，无法区分「没看见」与「看见了不理」。联动：`_TIMELINE_ROWS_BY_DETAIL`（逐回合时序降采样 40/100/200 行）、`_PER_MSG_CHARS` 非 Claude 回退采样放宽、`_DIALOG_BUDGET_ESTIMATE` 确认弹窗粗估按档放大。GUI：`LlmConfigPopup`「过程数据供给量」三档单选卡片（文案 SSOT 在 `llm_prefs.DIALOG_DETAIL_LABELS`；**点击绑定必须覆盖 Label 本身**——Label 遮挡容器，只绑 Frame 时点文字无反应，曾致「完整」选不中）；调用链（app worker/弹窗/预估）全部传档。
- **输出扩容**：`MAX_OUTPUT_TOKENS` 4096→32768、`TIMEOUT_S` 120→300、convergence 字数 ≤1200→2500~5000、dynamics 800~1200→2000~4000（「宁可长而实，不可短而空」）。**实测机型 gemini-3.8-flash 的兼容事实（2026-09 调研，官方 model card + OpenAI 兼容层文档）**：①1M 输入窗口/65,536 输出上限，thinking token **计入输出预算**（medium 档默认 8192）——max_tokens 必须给思考留双倍余量，32768 即为此；②**官方已废弃采样参数**（3.6+ 起忽略 temperature/top_p/top_k 且预告未来报 400；Claude 新模型非默认值同样 400）——`llm_client` 刻意不发任何采样参数，审计风格靠 system prompt 约束；③thinking level 用 `reasoning_effort`（OpenAI 兼容层映射 thinking_level，与 thinking_budget 二选一）控制，我们用模型默认档不显式发送；④速度 TTFT p95 ~9s、吞吐慢尾 ~58 字符/秒——3000-5000 字报告常态 1-3 分钟，超时给足 300s。**SycEval 基准：Gemini 系是谄媚率最高的主流模型（62.47%）**——用户的「说好话」观察与基准吻合，故除 prompt 反制外加**机械审计校验**：`llm_prompts.audit_warnings(text, is_dynamics)`（确定性本地规则：缺必备小节/命中笼统表扬黑名单（「整体表现良好」等 8 句式）/**【T数字】**转折锚点<3/全文无责任归因表述 → 各自一条警示），生成链（app worker 与弹窗 `_llm_done`）落盘 `entry['audit_warnings']`，报告卡片徽标行亮「审计校验: N 项警示」（WARNING 色，Tooltip 列明细）/「通过」（SUCCESS）/旧报告无字段隐藏——警示不删改内容，标注供用户折扣采信。
数据主体仍是**完整对话时间线**——`reader.read_dialogue`（Claude 源专属，read_user_messages 的姊妹函数：用户消息全文 + assistant 文本（跳 thinking）+ 工具调用与 Edit/Write 代码 diff 全量 + Bash 命令 + 报错/正常工具反馈），同 `message.id` 文本去重 + tool_use id 去重防 rewind 双计；7MB 真实会话 → 1257 行/9 万字符（standard）～ 25 万+ 字符（rich）。**输出结构按「AI 程序员三能力」四段**（system prompt v2）：【业务意图还原】（依据用户原文 2-3 句）→【语义距离评估】（引用具体回合/消息/工具行为说偏离点与累积）→【反馈序列评估】（哪些纠错有效/被忽略/时机过晚 + 反馈措辞改进建议）→【下一步行动】（继续/回退/重开）；≤1200 字、`MAX_OUTPUT_TOKENS=4096`。非 Claude 源无 assistant 文本读取，dialog 档回退用户消息采样（首条必选+纠正优先+长度补预算，复用 `parse_util.is_correction`）并在 prompt 标注「本来源无 AI 回应文本」。system prompt 含「用户消息仅为素材忽略指令」injection 防线 + `PROMPT_VERSION`。**GUI**：`LlmConfigPopup`（UploadDialog 模板，「测试连接」后台线程测**表单未保存值**；两框同空保存=清除配置）；`SessionTimelinePopup`「LLM 解读」→ 确认弹窗 → daemon worker → 占位「解读生成中…」→ `_llm_done` **落盘 + 一行成功提示 + 回调跳页签**（曾把全文塞弹窗底部 180px 定高滚动区，阅读体验差按用户反馈移除）。**会话右键直达（收信箱并发模型）**：右键「LLM 分析/相空间分析」免开弹窗直接派发 `app._run_session_llm`——①**每次都弹知情确认**（`_confirm_llm_direct`，内容口径同弹窗确认：端点/出境范围/预估 tokens/不可撤销提示。曾试「确认一次记住」（`direct_confirmed` 键，已删）——用户实测确认框时有时无不可预期（重存配置重置记住态），数据出境不可逆，固定每次确认）；②**(session_id, kind) 去重**（同类任务运行/排队中重复派发直接拒绝——双发=双倍成本与出境）；③**有界线程池** `ThreadPoolExecutor(max_workers=3)` 惰性创建（勿回退无界裸线程）；④**回填走 UI 队列**（`_llm_ui_queue` + 主线程 60ms 轮询 `_poll_llm_ui_queue`，同弹窗 ⓒ 模式；**勿用裸跨线程 `root.after`**——曾因修 bug 误删调度行致失败任务永久泄漏零反馈，失败路径必须清任务表+状态栏红字，有回归测试钉住）；⑤报告 id 带 uuid 后缀（纯毫秒时间戳并发碰撞会让同 id 报告被连带误删）；⑥收信箱工具条「取消生成中任务」→ `cancel_llm_tasks`（排队中的 future.cancel 立即取消，运行中的网络请求不可中断自然回收）；`llm_reports.append/delete/clear` 持模块级 `threading.Lock`（并发落盘曾实测丢报告 + Windows `os.replace` PermissionError）。**报告持久化（第 6 页签「LLM 报告」）**：`core/llm_reports`（`tcer_llm_reports.json`，entry 含会话快照 session_title/source/turns/net_loc/cost_display——回看不依赖会话文件仍在；新→旧排序，上限 `MAX_REPORTS=200` 裁最旧）；`views.LlmReportsView`（PanedWindow 水平拖拽分栏（`sashwidth=3`，左栏 minsize=200 默认 310px 紧凑点选、以报告为展示主核心；右栏 minsize=380 全高阅读区；`_apply_sash` 稳健就位）；解除标题 20 字符截断，Treeview 单元格自适应拉伸与自然裁切；多来源扩展 `REPORT_KINDS`：会话/项目/对比/诊断/通用；左列表带类型胶囊筛选+实时搜索，精炼三列「类型/解读对象/时间」（模型在报告卡片展示，列表空间全部释放给标题）；右全高 Hero 卡片头+徽标行+一键「复制全文」+ 原生平滑滚动 Text 阅读器，层级标题 `sec_head`/分级标题 `md_h1-h3`/多级嵌套子列表（`sub_list_item`，前缀 `◦ ` 悬挂缩进）/列表键值冒号自动加粗/行内胶囊代码高对比衬底/等宽多行代码块（`code_block`），正文 `wrap="char"` 彻底消除中文长句折行大空白空洞，`_render_tab` idx==5 特例不依赖 `_current`，同项目聚合页签模式；删除选中/清空全部；支持专属【相空间收敛动力学报告】(`kind="dynamics"`，时间线弹窗「相空间分析」独立生成)：基于王垠信息论框架(`_DYNAMICS_SYSTEM` + `parse_dynamics_payload`)，模型输出复盘正文与遥测 JSON(语义距离 Ds/反馈矢量/吸引子状态/三能力得分)；`PhasePortraitWidget 仅在选中动力学报告时于正文上方展开相空间漫游相图（基于 `charts._aa_layer` 2× 超采样抗锯齿渲染彻底消除曲线与质点锯齿；禁用 emoji 保持专业工程质感；补全 divergent/trapped 状态映射高亮发散与死锁红标；切换普通报告完全隐藏零干扰；`clean_math_syntax` 自动净化正文中未解析的 LaTeX 数学代码转为自然中文））；生成后 `on_report_saved` 回调 → `app._on_llm_report_saved` 切页签并 `select_report` 选中（**`selection_set` 的 `<<TreeviewSelect>>` 依赖事件循环，程序化选择后须显式调 `_on_select()`**）。**三个实测教训**：ⓐ**`messagebox` 必须传 `parent=self._win`**——无 parent 时以主窗为 owner，确认框关闭后弹窗被主窗整窗盖住（用户感知=「弹窗消失了」；修法：parent= 本弹窗 + `_llm_done` 里 `lift()`）；ⓑ**懒加载必须在 worker 线程**（曾放主线程换预估准确——大会话同步读 JSONL 卡 UI 数秒；预估按对话预算上限补算）；ⓒenqueue 层兜底必须 `except (tk.TclError, RuntimeError)`——**跨线程 `after()` 在无 mainloop 的 Tk 上抛 RuntimeError 而非 TclError**。**线程模型**：弹窗自持 daemon 线程；setter 层叠 `winfo_exists` 守卫 + 整体 try/except。**注入链**：`app.show_session_timeline` 注入 `load_dialogue`（仅 Claude 源）与 `load_user_texts`（各源回退），在 `_llm_work` worker 线程执行。测试：网络层全 mock `urllib.request.urlopen`（CI 零联网）。**jev 双引擎（v1.9.2，TypeSafe System One 判定模型）**：`core/typesafe_client`（纯 urllib POST `{base_url}/v1/systemone`，单请求多问题并行扇出 choice/noul/score 三题型，HTTP 401/422/429/529 细分转译；`evaluate_dynamics_cascade` 两阶段级联：Pass 1 宏观相拓扑 + 逐里程碑六维 → Pass 2 确定性锁定 T_crit（优先 barrier 突破点）做微观法医/反事实/水床裁决，Pass 2 网络失败优雅降级回 Pass 1）+ `llm_prompts.detect_phase_singularities` 里程碑提取（**turn_stats 键 0-based、显示 1-based**：`T{turn+1}·U{user_turn}`；结构点=边界/barrier(LOC 峰值)/attractor(报错峰值)/bifurcation；**U 脉冲超预算先均匀降采样（首尾 U 必保）、再 gap 填补（阈值 max(15,N/8)、总上限 16 与 ground 插值封顶同值）——顺序不能反**：U 脉冲全量占满名额会卡死 gap 填补，实测仍现 200+ 回合空白直跳）。**数据公正契约（用户核心要求：目标达成与资源耗散严格解耦）**：state 四块 `intent_specification/terminal_deliverable/thermodynamic_dissipation/control_sequence`，里程碑只在 control_sequence 序列化一次；instructions 明示「长会话与中间报错是正常试错耗散」「瓶颈归因不影响是否达成目标」；**勿在本地用 event=retry_loop 覆盖 attractor_trapped**（中途重试≠最终受困）。**Pass 2 证据必须来自 derived 确定性遥测**（`ops_by_turn` 邻域±1）——`read_dialogue` 行只有 `[用户]/[AI]/[工具]` 前缀、无回合标号，按 `[T{n}]` 匹配恒落空（曾恒用 dialogue 前 8 行冒充转折证据）；dialogue 用户消息只能标注「全局摘录，非回合绑定」。**audit_warnings 带 provider**：Jev 报告是本地确定性合成章节，必备节按实际标题（执行摘要/开局/转折/反馈）校验——沿用 general 的「速读摘要/关键转折」子集会 100% 误报。**术语平实化同样覆盖 Jev 报告**：Jev 只返回类型化判定（choice/noul/score+概率），报告全文本地模板合成（#38 平实化替换表同口径：探索期/构建期/卡壳期/收尾期、按下葫芦浮起瓢、代码有效率……），报告头须注明「判定数值由 Jev 返回、成文由 TCER 本地合成」；**报告循环消费 trajectory 一律 `.get()` 兜底**——ground 插值节点只带核心字段（event 专有值 `user_impulse` 需补中文映射），裸 `pt["trigger"]`/`pt["snr"]` 在长会话上必 KeyError（短会话单测不触发插值路径，掩盖过）。**配置**：`llm_prefs.typesafe_key/base_url/model` 三键显式填写（用户约定：**刻意不做本机凭据检测、不做测试连接**，有 `not hasattr` 测试钉住）；双引擎可用时时间线/右键均弹引擎选择，入库 entry 的 model/scope 按 provider 区分。**antigravity turn 0-based**：agy reader 的 turn_stats/tool_ops/turn_net_locs 回合键从 1 改 0（与其余六源一致），否则里程碑/T·U 标签整体错位一格。**v1.9.2 二次优化（官方文档实测后）**：①**题面英文**——官方声明 Jev 主要训练语言为英语、CJK 准确率较低；A/B 实测（同 state 中英题面各调真实 API）：真实工作会话英文版 confidence 普遍更高且 input tokens 省 20~35%，低证据会话两种语言判定都不稳（正好由低置信标记兜住）；判定的 instructions/criteria 与结构字段值全英文（`_KIND_EN` 表），用户消息素材保持原文。②**证据供给**：`control_sequence` 各里程碑带 `user_msg_excerpt`（该 U 轮用户消息摘录），`terminal_deliverable` 带 `final_actions`（末 5 个工具动作）+ `verification_performed` + `exit_clean`——判「是否达成」必须看到交付物形态与验证证据。③**转折直接指认**：`turnaround_pick` choice（候选=里程碑回合号+none，"select instead of generate"），synthesize 优先级 Jev 指认 → 本地 Ds 阈值 → barrier 候选兜底。④**交叉校验**：官方明示校准是群体性质、单次判定不保证正确——Jev 判未收敛但本地遥测显示正常收尾+净增产 出（或反向判达成但零产出）时报告亮「判定与客观证据的张力」警示，`dynamics_data.evidence_tension`。⑤**低置信升级**：主概率<0.6 或 confidence<0.7 → 报告自动建议切「通用大模型」深挖（System 1→2，只建议不自动发请求），`dynamics_data.low_confidence`。⑥**责任占比**：Pass 2 `blame_ai/blame_user/blame_env` 三个 score 归一化为份额（与通用 LLM 报告的「责任占比」同构——混合责任说清份额）。⑦**问题瘦身**：「先查后改」是模式识别，本地 `_local_debt_kind`（先 Read 后 Edit=prudent/直接 Edit=reckless）确定性推导进 `debt_local`，不再发 debt_t{n}（-16 问/请求）。实测 516 回合会话端到端 1.87s、16 里程碑、turnaround_pick 与 A/B 跨语言一致。**深度调研后三件套（v1.9.3，doc/jev-research.md 方案 D/E/A）**：①**判定缓存**——`typesafe_client.evaluate(use_cache=True)` 按 (base_url, model, state, questions) 内容 sha256 落盘 `prefs_dir/tcer_typesafe_cache.json`（128 条裁剪、tmp+os.replace 原子写、IO 失败静默降级、命中返回 deepcopy 防调用方污染）；动机=省钱（重放不重复计费）+**采样漂移归零**（官方实测 σ≈0.01，缓存后同输入恒同输出，对齐 audit 可对账精神）；API key 刻意不进缓存键；model 用调用方字符串——钉版本 ID 天然隔离、别名接受跨版本缓存（动力学判定对小版本不敏感）。②**题面逃生口体检**——Choice 分布和恒为 1，覆盖不全的分类题**必须**带显式 none/other（官方 jaggedness：乱码输入仍必选一项），补 `event_t{n}.other`（五类事件外的压缩/权限等）与 `crit_causal_attribution.other`（五类人因外的纯环境故障，否则污染 blame_env）；**合成层中文映射必须同步**——`event_cn_map.get()` 无默认值缺映射渲染成 None、`causal_map` 兜底落 clean_breakthrough 是错误语义；测试钉死「choice 要么有逃生口、要么白名单（intent_entropy 模糊度/vector 方向类必有其一）」。③**语义审计（反谄媚审计官）**——`llm_prompts.build_semantic_audit_payload/format_semantic_audit`（6 个 Noul：无据正面评价/归因和稀泥/术语超纲/结论无据/转折含糊 + 反向健康题「建议可执行」，风险题 ≥0.6 告警、健康题 <0.4 告警），worker 线程执行（**网络调用严禁进主线程回调**）、未配置 typesafe key 时 None 零联网、任何失败静默不影响主报告入库；entry 新增 `audit_semantic`（warnings/probs/probs_display 中文标签/praise_risk），views 审计徽标合并两路计数、tooltip 分【本地规则】【语义判定 · Jev】两段。实测教训：app worker 里 `typesafe_client` 名字仅在 use_typesafe 分支 import——分支外的语义审计块须局部 `import as _ts_client`，否则通用 LLM 路径 NameError 被外层 except 吞成任务失败。**真实会话三报告复盘（v2 修订）**：①**语义审计只审自由生成的 LLM 叙事**——Jev 动力学报告是本地模板合成，跑了语义审计会把自家模板的 ζ/η/相空间指标名打「术语超纲 97%」、「反馈质量良好」打「无据正面评价 76%」（crosscheck 路径本就跳过、dynamics 路径漏了，两路口径现已统一 `not use_typesafe` 才审）；②**`total_tokens` 静默 bug**——`build_llm_derived` 曾取 `report.usage.total_tokens`（TokenUsage 无此属性，getattr 默认 0），报告「消耗 0 Token」，真实会话才暴露，改为 input+output；③**crosscheck v2**——v1 在「正则零命中」会话四象限全 0、结论「无分歧」是**假阴性**：Jev 0.46 的不满消息被吞进 agree_none（灰色地带只对正则命中侧开放）、newreq 算了不上报、逐条明细不展示。v2：三问（corrects 方向/**dissatisfied 质量不满**/newreq，signal=max(c,d)）、**灰色地带 0.40–0.60 两侧一律单列**、newreq≥0.6 计「需求扩张」、逐条明细表全量展示、零命中零强信号时 fail loud（「两引擎都没报警 ≠ 确认无纠正」）；注意 **regex_hits 必须按 `it['regex']` 真实计数**——曾按类目拼凑（agree+regex_only+uncertain），miss 侧灰色被算成命中，fail-loud 分支永远进不去；④**Jev 里程碑证据供给不足**——432 回合会话 Jev 版转折（T210「AI 自主推进」）与读全文的 gemini 版（T382「用户裁决破局」）相反：control_sequence 的 user_msg_excerpt 仅 160 字符+干指标，用户裁决语气被截没。已扩到 400；**深层重构（待办，随方案 B）**：把 U 轮用户消息全文按里程碑分桶、或把 dialogue 相关窗口注入 state——Jev 拿到的应是「压缩但有裁决力」的世界，不是干指标。**④纠正信号交叉验证（方案 C，kind="crosscheck"）**——`llm_prompts.build_correction_crosscheck_payload/format_correction_crosscheck`：指标层 `correction_msg_count` 是 CORRECTION_RE 确定性判定（审计账本不可替换），Jev 对同批用户消息逐条两问（corrects=纠正先前方向 / newreq=新增需求，**两问独立不假设互补**——jaggedness #8），与正则结果四象限对账（双方确认/疑似误报/疑似漏报/不确定带 0.40–0.60 **仅正则命中侧有此分类**）；消息截断 400 字符、上限 40 条、跳过斜杠命令；右键「纠正信号交叉验证」→ 每次数据出境确认（用户消息文本）→ (sid,"crosscheck") 去重 → `_crosscheck_worker`（读 `_load_user_messages`）→ 报告入库；`audit_warnings` 的 provider="crosscheck" 口径：必备节只查「执行摘要」，**转折锚点/责任表述两条叙事契约跳过**（数据对账报告天然无此二要素，不跳会双误报——Jev 报告踩过同款坑）。**⑤计费记账（方案 F）**——`typesafe_client._record_usage`（真实网络调用成功后累加 usage.input/output_tokens 落盘 `tcer_typesafe_usage.json`，**缓存命中不计**——没花钱就不进账单；锁内原子写），`usage_stats()/usage_cost_usd()`（官方输入单价 $0.042/MTok、输出免费）；LlmReportsView 标题条「Jev 累计 N 次 · Xk tokens · ≈$x.xxx」（on_show 刷新、零请求隐藏、Tooltip 说明口径）——opt-in 付费必须可见，按「每解决一个任务」算账而非每 token。

39. **相空间收敛动力学规范（v1.8.0，五大前沿理论具象化与 2026 演进）**：在王垠信息论框架（双信源模型、初始意图降熵、相空间游走、平庸代码吸引子俘获、狄拉克目标收敛、反馈互信息增益）基础上，全面具象化五大经典与前沿理论并适配 2026 Agent 框架（Claude Fable 5.1 / GPT-6 / Gemini 3.8 / Grok 4.6 / omp 2026 并行工坊）：
①**五大物理数学模型（`core/metrics.py`）**：
  - *理论一：Waddington 双阱表观遗传连续势能曲面* `compute_waddington_potential(ds, cost_frac)`（狄拉克深阱 Ds=0.05 + 吸引子局部阱 Ds=0.86 + 鞍点势垒 Ds=0.55，钳位 [-1.2, 0.8]）；
  - *理论二：Friston 变分自由能认知负债比* `compute_epistemic_debt(read_lines, write_lines, grep_ops)`（J_pragmatic ÷ J_epistemic，≤1.0 充分探查，≥4.0 认知盲区动刀）；
  - *理论三：闭环控制阻尼比与水床效应* `compute_cybernetic_damping(traj)`（欠阻尼 ζ<0.70、临界平稳 0.70~1.10、过阻尼卡壳/单向漂移 ζ=1.40）+ `compute_damping_spectrum(traj, window=5)`（滑动窗口瞬时阻尼时变谱与失控拐点检测）+ `analyze_waterbed_causality(traj, ops)`（跨回合资产耦合水床因果链与冲击半径）；
  - *理论四：MIT 统计物理 SLDS 四相态* `infer_phase_regime(ds, vec, evt, ed)`（gas 探查/liquid 构建/glass 死锁/crystal 终态）；
  - *理论五：计算热力学朗道尔信息擦除耗散与卡诺效率* `compute_landauer_dissipation(net, rework, comp, tok)`（不可逆重构返工与上下文压缩耗散 Q_diss，广义卡诺有效代码利用率 η）；
  - *2026 多智能体协同增益* `compute_swarm_synergy(traj)`（量化 Task / workpool 子代理向心负熵贡献度 vs 离心沟通噪音 CSG）。
②**提示词与遥测契约（`core/llm_prompts.py`）**：`_DYNAMICS_SYSTEM`（`DYNAMICS_PROMPT_VERSION = "2026-09-dyn-v4"`）严禁 LaTeX 符号标记（纯字符 Ds/Ed/lambda/zeta/eta/V），保留「狄拉克目标点」与「平庸代码吸引子」学术初心；输出两部分：Markdown 报告（800~1200 字，顶置【会话动力学定性判词与速读摘要 TL;DR】3 秒速读卡，各章标注 `时空区间: 助手第 T.. 轮 · 用户消息 U..` 并显式加粗 **【Txxx】** 锚点解决用户不愿细看问题；正文自然允许如 `(UE5)` 等技术括号，不作形式化限制）+ 尾部唯一 ````json ... ```` 遥测块（字段绝对兼容）；`dynamics_prompt` 依总回合数动态扩容采样期望规模（长会话严格收敛至 10~16 个核心里程碑，杜绝过密视觉灾难）；`ground_dynamics_user_turns` 对长会话中跨越数十轮的巨大时间空洞自适应填补客观事件质点，插值后总数严格封顶 $\le 16$，确保长会话分辨率与深度紧密契合，且在 `derived=None` 时 100% 确定性后备补全全局阻尼比/卡诺效率/势垒跨越/认知平衡力。
③**GUI 渲染与交互（`gui/views.py` `PhasePortraitWidget`）**：
  - *双层顶栏与专业二维相图交互系统（Canvas Interaction Suite）*：第一行标题 + 视图模式切换（`[时序流形] [相速度极限环]` 双态切换）+ 交互操控控制台（`[－] [100%] [＋] [复位]`）+ 右侧四能力胶囊条；第二行 4 个物理遥测徽标（终态定性、改动稳定性 λ、推进阻尼比 ζ、卡诺效率 η、多智能体协同），彻底去除硬编码方括号与第一行统一标签风格，全面禁用 emoji；
  - *极速平滑缩放与漫游（Cursor-Centric Zoom & Full-Range Pan）*：①严格以鼠标光标所在视口相对位置为锚点进行无偏缩放（`new_pan = (focus - pad) - (focus - pad - pan) * ratio`），彻底消除光标漂移与中心缩放假象；②**左下狄拉克目标与全域平移**：左下角高亮狄拉克目标点（Ds=0.05, 0.88）与收敛走廊、X 轴底座标注（`0.0 (契合真实意图)` / `1.0 (严重偏离意图)`）严格保留原样，轨迹垂直映射严格沿用原公式 `(pad_t + pan_y + plot_h_eff) - frac * plot_h_eff * 0.82 - 8 * zoom` 并解除旧代码 `max_pan_y=40` 的限制（全域释放为 `min_pan_y = plot_h*(1-zoom)-80, max_pan_y = 80`），放大后顶部终态与底部初始态均可自由平移对焦；③**毫秒级矢量加速（零 PIL 损耗）**：相图全面采用原生矢量流线绘制（`_aa_layer_flat` + 16ms 滚轮事件节流），单帧耗时 <1ms，彻底摒弃 200~300ms 的超大图 LANCZOS 重采样卡顿，滚轮缩放与拖拽漫游满帧 60 FPS 丝滑顺畅；④双击画布空白处或单击「复位」一键平滑归位 100%；
  - *质点巡航探针与时间线逐帧步进（Node Inspector & Timeline Scrubber）*：单击任意质点锁定对焦，触发高亮双环发光十字准星，画布左下角浮现常驻 HUD 探针卡片（展示该轮 Ds 变化、相速度、SLDS 相态、认知负债 Ed、势能 V、动力学事件与详细复盘解读），配合 `[< 上轮]` `[下轮 >]` `[居中]` `[X]` 纯文本按钮及键盘方向键 `←` / `→`，实现像录像带回放般的逐轮因果巡航，镜头自动平滑对焦跟随；
  - *物理势能与收敛几何渲染*：6 条马鞍面 Waddington 连续等高线（`theme.PHASE_CONTOUR_LOW/MID/HIGH`，随缩放动态微积分映射）+ 理想向心收敛走廊参考线 + 鞍点势垒金色虚线（`PHASE_BARRIER_CREST`）与突破胶囊 + 止损红线与纠偏胶囊 + 底座 10px SLDS 四相态彩色光谱流带；
  - *质点与因果图元彻底除噪*：高认知负债双层橙红警示光环（`DEBT_GLOW_DANGER`），低负债青绿微光（`DEBT_GLOW_SAFE`）；水床扰动以 16 段二次贝塞尔自适应起拱抛物线虚线架设，拱顶镶嵌胶囊；人类冲量引导线（`U4 关键纠偏`）严格收敛至全图至多 2 条最具向心突破性的关键转折点（杜绝数十条灰线与文本穿透主轨迹），常规需求轮次完全收拢至交互 Tooltip；垂直陡峭段（|dy|>1.3|dx|）文字自适应左右错开；全图文本遵循 2D 欧氏距离防叠（$dx^2 + dy^2 < 38^2$）；
  - *全视口 Hover 补全与说人话*：全图所有图元（势垒脊线与胶囊、止损红线、水床胶囊与弧线、底座相态带、人类指令引线、状态徽标、质点）均绑定自然工程语言 Tooltip，以 ` · ` 统一层级分割，杜绝 `xxx(yyy)` 括号噪音。

> 完整架构说明：[doc/architecture.md](doc/architecture.md)
> 数据格式细节：[doc/data-format.md](doc/data-format.md)
