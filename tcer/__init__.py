"""TCER — Token-to-Code Efficiency Ratio offline metrics.

Thin package shell. The real code lives in ``core/`` (metrics, reader, loc, ...)
and ``gui/`` (Tkinter desktop interface). Run with ``python -m tcer``.
"""
from __future__ import annotations

__version__ = "0.3.0"
