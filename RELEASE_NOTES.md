# TCER v1.0.16

本次聚焦「用户消息」子窗口在项目视角下的可读性：聚合查看时现在能清楚看到每条消息来自哪个会话。核心 TCER / CTEI / 成本口径不变。

## 改进

### 用户消息聚合视图标注会话来源
在项目视角点开「用户消息」，此前是所有会话的消息平铺在一起，无法区分来源。现在按会话分组，每组前显示一条来源标识：

- 格式为 **`日期 · 会话标题 · sessionid`**（例：`2026-07-30 · 扩展项目来源图标方案 · 2162e1ca-0c5…`）。
- **日期**取会话开始日（本地时区 `YYYY-MM-DD`），无时间戳的旧会话自动省略日期段。
- **会话标题**截断到 24 字符、**sessionid** 截断到 12 字符，两者均限长，避免来源条过长换行。
- 汇总头追加「· N 个会话」；无消息的会话自动跳过。

单个会话视图行为不变（仍是扁平消息列表）。六个数据源（Claude / Codex / OpenCode / Grok / omp / Pi）统一适配。

## 说明

- 仅 GUI 展示层改动，不涉及 reader / 计量口径；闭环审计口径不变。

---

**完整变更**：`v1.0.15...v1.0.16`