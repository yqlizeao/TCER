# TCER v1.0.12

本次更新聚焦指标准确性：修复基准值散落硬编码导致的雷达图刻度失真，收敛评级/基准到唯一真理源（SSOT），并清理术语说明中的过时表述。

## 修复

### 效率雷达刻度失真（重要）
六维效率雷达的 TCER / CPE / CTEI 三个轴此前硬编码了旧基准（`76.59` / `8.22`），而实际生效基准早已迁移到配置文件（当前 `26.22` / `13.62`）：

- 导致雷达归一化刻度失真，多边形形状与真实相对水平不符。
- 「保存个人基准」后雷达刻度不跟随更新——旧硬编码值把它悄悄架空。
- 现改为构建时从唯一真理源实时取值（`metrics.TCER_BASELINE` / `CPE_BASELINE` 与 `GRADE_BANDS`），基准一改立即生效。

### 术语说明的结构缺陷与过时表述
- 修复「LOC 统计假设」条目因缺少逗号退化成畸形二元组的隐患（任何按三元组解包的消费方都会崩）。
- 移除与「纯离线仅析会话数据」定位冲突的过时表述（如「代码库累计行来自扫描工作目录」——该磁盘扫描早已移除），并补充 `originalFile` 回溯修正的说明。

### 导出评级图例改为 SSOT 派生
Markdown 导出的 CTEI 图例（优秀/良好/中等/低效/极端低效阈值）此前是硬编码字符串，现从 `GRADE_BANDS` 派生，评级体系一改，导出永不再漂移。

## 改进

- **基准提示澄清**：G6「TCER 基准 / 成本基准」两格的提示不再写死具体数字，改为指向上方实时显示的生效值，消除与实际配置的矛盾。
- **文档同步**：`doc/metrics.md` 明确基准以配置为唯一真理源，算例中的基准常量标注为演示值。
- **回归护栏**：新增两项测试锁定上述问题——雷达轴刻度须等于 SSOT、术语条目须为规范三元组。

---

**完整变更**：`v1.0.11...v1.0.12`