# TCER v1.0.8

本次更新聚焦「会话定位」与「视角辨识」两点体验改进。

## 新功能

### 会话搜索支持模型模糊匹配
会话列表的搜索框现在除按**标题、会话 ID** 过滤外，新增**按模型模糊匹配**：输入 `grok`、`sol`、`opus`、`sonnet`、`codex` 等模型名片段，即可一键筛出使用了该模型的全部会话。

> 用户通常记得某次对话用过的模型，却想不起具体事项或标题——此功能正是为此而做。匹配会话用过的全部模型 id（含多模型混用的会话），不区分大小写，并与红旗过滤、置顶排序正常叠加。

## 改进

### 会话 / 项目视角颜色区分强化
针对「经常忘记当前处于会话视角还是项目视角」的反馈，本次用统一的颜色体系强化区分（蓝 = 会话、橙 = 项目）：

- **上栏切换按钮**：会话视角 = 蓝色、项目视角 = 橙黄色（此前两者选中均为蓝色，难以辨别）。
- **指标分类、模型对比页签**：挂上彩色视角图标——会话视角显示蓝色双气泡、项目视角显示橙色文件图标，随视角切换实时变化，一眼可辨。
- 综合效率分排名、趋势页签保持原样，不参与视角着色。

> 受 ttk 限制，页签文字无法单独染色，故以「彩色视角图标」实现，仅作用于具备双视角的两个页签。

### 视觉微调
- Codex 来源图标改用 openai-dark，深色主题下对比度更高、更醒目。

## 其他
- 内部：将「web」概念整体重命名为「server」并清理中间文档，对使用无影响。

---

**完整变更**：`v1.0.7...v1.0.8`
