# TCER v1.9.4 更新说明

**团队术语库与语义判定层(SharedBrain MVP)**

---

## 一句话总结

在效率度量之外,TCER 新增第 7 页签「术语库」:把团队黑话、专有概念、跨职能误解做成可共享、可版本化的词条库,并提供三大语义能力——从会话历史挖掘术语、按黑话反查词条、检测一段需求文本的误解风险。

---

## 新功能

### 1. 术语库工作台(第 7 页签)

- **词条模型**:每条术语包含主标签、英文标识、别名与俗称、概念层面(机制/动态/体验)、状态(活跃/草稿/已废弃)、负责人、释义、**各职能怎么理解它**(美术/策划/交互/程序/音频/QA 六个视角各自的说法)、**常见误解**(哪个职能容易把它误读成什么、真相是什么)、备注。
- **词条库是一个 JSON 文件**,默认存于本机配置目录;可在「操作 → 切换词条库文件」指向**团队 Git 仓库中的文件**——词条库本身可团队共享、可版本化,这是特性不是漏洞。
- 表单全中文、实时校验、显式保存;支持状态筛选、实时搜索;可导出 JSON 库与 Markdown 对照表;可从其他来源粘贴导入词条片段(导入前列出全部条目明细,确认后才写入)。
- **不配置任何 API Key 时,以上全部功能零联网可用。**

### 2. 术语考古(会话右键「术语考古」)

从一次会话的用户消息中沉淀团队用语:

- **离线模式**(未配置 Key):统计词条库已知术语在历史消息中的命中热力图与上下文摘录。
- **双引擎模式**(配置通用 LLM + TypeSafe Key):AI 从消息中提名新词候选(上限 15 个)→ Jev 引擎逐候选判定它是「已有词条的别名 / 值得收录的新词 / 日常噪音」→ 产出词条提案(JSON 片段)。在术语库页签「从考古报告合入新词」即可载入提案、确认后入库为草稿。
- 状态栏分阶段反馈进度(LLM 提名 → Jev 归属判定),完成或失败都有明确提示。

### 3. 反向查询(操作 → 按名称或黑话查词条)

输入一个黑话或简称:本地词条库直接命中则展示词条卡;本地没有且已配置 Key 时,可经 Jev 语义推断它最可能是哪个已有概念(高置信直接命中,中置信列出候选供点选,并带「非术语」逃生口)。

### 4. 歧义检测(操作 → 检测一段话的误解风险)

粘贴一段需求文本:自动识别其中涉及的词条,对每条「常见误解」生成探针做语义概率估算——任一误解风险超过 50% 即标红报警,40%–60% 灰色地带单列;报告含各职能正确理解卡。未配置 Key 时降级展示词条预设的误解清单。

### 5. 报告收信箱集成

考古与歧义检测报告进入「LLM 报告」页签,带类型胶囊筛选;考古完成后自动跳转并选中该报告。

---

## 隐私与安全

- 未配置任何 Key 时**零联网、零联网入口**;配置后每次网络调用前**逐次弹窗确认**(端点、出境范围、预估消耗、不可撤销提示),不提供"记住选择"。
- 术语库、判定结果、报告**永不参与 CTEI、综合效率分、任何指标与 CSV 导出**——审计闭环的客观性不受影响。
- AI 产出的词条永远是**提案**,合入前列出明细由您确认(「AI 提案、人裁决」)。
- Jev 调用沿用既有计费记账与判定缓存:成本可见,重放不重复计费。

---

## 本版修复的重要问题

本批功能首版交付时存在多处「测试全绿但真实使用即崩」的缺陷,已在发布前全部修复并逐一钉死回归测试:

1. **术语考古入口一点就崩**(调用了不存在的配置检查函数)。
2. **配置 Key 后考古必崩**(网络调用函数名与参数不匹配)。
3. **歧义检测完全不可用**(参数顺序颠倒、幽灵参数、图标未导入三连)。
4. **考古卡在「正在进行 Jev 归属判定」永远不动**——两个叠加原因:回包键名对不上导致解析崩溃;错误回填因 Python 异常变量作用域问题被静默吞掉,失败提示永远显示不出来。修复后任何一环失败都会给出明确的状态栏提示。
5. **考古报告合入只导入一个词**——报告里 12 份提案只提取了第一份;已改为全量提取并增加导入前确认清单。
6. **考古完成没有任何反馈**——完成后现在自动跳转「LLM 报告」页签并选中该报告;空词条库的离线报告会给出明确的可行动指引。
7. **测试污染用户数据**——修复了一个测试基建缺陷:此前每轮全量测试可能向真实报告文件写入测试数据。

---

## 界面与体验

- 术语库界面按项目样式规范全面整改:全中文文案(不再出现 slug、MDA、F2/F3/F4 等字段名与内部代号)、统一深色下拉组件、输入槽底色、白线图标体系(书/加号/保存)。
- 右键菜单「术语考古(本会话)」简化为「术语考古」。

---

## 质量验证

- 全量 **752 项测试通过**;其中新增 41 项术语库功能测试与端到端闭环测试(构建完整界面 → 模拟真实操作 → 验证落盘与报告入库,含双引擎与失败路径)。
- 闭环审计 43 个项目零失败。
- 使用真实会话数据(12MB JSONL)与真实 Jev 引擎回放验证全链路。

---

## 升级说明

绿色免安装,覆盖运行即可;词条库等新配置文件与既有数据互不影响。设计依据与实施规格归档于开发侧调研目录,不随本仓库分发。
