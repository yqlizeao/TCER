# TCER v1.1.1

排名页「洞察与意见」面板的视角命名与切换微调（承接 v1.1.0 的综合效率分 v2 + 洞察面板）。

## 改进

- **视角命名统一**：洞察面板改用与「指标分类」页签一致的 `(项目)` / `(会话)` 后缀，
  弃用之前的「（跨会话）」表述——未选会话时标题为「洞察与意见 (项目)」，
  选中会话后为「洞察与意见 (会话)」。
- **选中即切视角**：在排名表选中某个会话后，面板从项目视角洞察整体切换为该会话的
  会话视角洞察（项目视角区块移除），与页签的项目/会话切换语义一致。

## 说明

- 纯 GUI 文案/视角切换调整，综合效率分与洞察引擎（`core/insights.py`）逻辑不变。
- 全套 462 passed, 1 skipped（含更新后的洞察面板渲染护栏测试）。

---

**完整变更**：`v1.1.0...v1.1.1`
