# TCER v1.0.15

延续 v1.0.14 的主线（把已落盘却未采集的信号接线到既有指标）：本次把 **omp / Pi 的三个 prompt 行为信号**从「不适用」接成真实数据。核心 TCER / CTEI / 成本口径不变，闭环审计 41/41 通过。

## 新增数据接入

omp / Pi 的用户消息与 Claude 同构（`message.role == "user"` 的文本块），此前只采集了消息计数与图片输入。现补齐三个 prompt 行为信号：

- **斜杠命令**（`slash_command_count`）：以 `/` 开头或经命令面板（`<command-name>`）发送的用户消息数。
- **纠正消息**（`correction_msg_count`）：含显式纠正措辞（不对/重来/撤销/undo…）的用户消息数。
- **首条消息长度**（`first_prompt_chars`）：首条真实用户消息的字符数——任务描述投入度。

这三个指标此前对 omp / Pi 显示「不适用」，现变为真实数据（在真实 `~/.omp` 数据上已验证可提取）。

## 修复

### 「项目记忆文件」指标在会话视图显示不出数据
项目记忆文件是**项目级**指标，此前只挂到项目汇总报告，切到单个会话视图时网格显示「-」并被折叠进「N 项无数据或不适用」，但点击后的子窗口仍列出记忆文件——网格「无数据」与弹窗「有数据」自相矛盾。

- 根因：`memory_files` / `memory_dir` 只写到聚合报告，每个会话报告为 `None`。
- 现改为把这项**不随会话变化的项目级事实**统一挂到聚合与每个会话报告，网格、弹窗、导出在「项目汇总」与「会话」两种视角下一致显示同一计数。

## 一致性 / 内部

- 斜杠命令与纠正措辞的判定逻辑（正则 + 分类器）从 Claude reader 私有实现**上移到 `parse_util` 作跨源 SSOT**，Claude / omp / Pi 共用同一张正则，杜绝各 reader 独立维护导致的口径漂移。
- 子代理的用户消息是 Task 派发 prompt、非真人输入，采集时**跳过**这三个信号（与 Claude reader 的 `is_sub` 守卫一致），token / LOC 等真实成本照常并入父会话。

## 说明

- 不改动 TCER / CTEI / 成本等核心计算口径；闭环审计口径不变（41/41 通过）。

---

**完整变更**：`v1.0.14...v1.0.15`