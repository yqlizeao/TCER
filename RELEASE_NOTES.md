# TCER v1.2.0

效率榜双视角重做 + 洞察面板扩充（新增「金额」类）+ 个人基准彻底重构。

## 效率榜（原「综合效率分排名」）

- **页签更名**为「效率榜」，双视角本质区分：
  - **项目视角**：右栏为项目实体画像（项目聚合分/等级/评分覆盖率 + 三轴含会话离散度 + 项目级系统性洞察）；左表为「贡献归因榜」，新增「贡献Δ」列（会话分 − 项目聚合分），按 Δ 排序，一眼看出谁在拉高/拉低项目分。
  - **会话视角**：左表为名次榜，右栏为选中会话的三轴构成 + 会话洞察。
- **视角切换单一入口**：仅左上角分段控件切换视角；点排名行只选中会话、不再翻转视角（修复点行卡死的重入死锁）。

## 洞察与意见面板

- **规则扩充**：单会话诊断规则从 8 条增至 16 条（探索比、Bash 占比、文件反复重改、改完验证闭环、测试覆盖、上下文窗口占用、缓存回本、反复纠正等），全部 grounding 到真实字段。
- **文案说人话**：建议改为「照着做就行」的具体动作，去术语。
- **新增「金额」类**（橙黄色）：花钱相关洞察单列，语气俏皮、以「值不值」为视角而非一味省钱（单次花费、单位成本 vs 基准、返工烧钱）。
- **排版修复**：动态换行随容器宽度自适应（消除固定 300px 造成的无意义换行 + 右侧留白）；折叠分组正文锚定到各自标题下方（修复展开后正文错位）。

## 个人基准

- **彻底重构校准弹窗**：先开窗、后按需校准；上下布局、无冗余取消按钮；操作按钮紧随标题。
- **两种模式**：基于全部会话（跨项目统一基准，写全局）/ 逐项目分别校准（每个项目单独基准，打分时优先生效，回退全局）。逐项目列出全部项目，样本不足者标注跳过原因。
- **计算选项**：离群过滤做成默认开的可见开关（剔除近零产出导致成本失真的会话）；新增计算方法（中位数/平均数）。
- **基准与时间范围无关**：始终基于全部历史会话。
- **有效会话阈值** `MIN_BASELINE_SESSIONS` 从 10 降为 5。
- 指标名/单位全部对齐指标 SSOT（`TCER（行/百万）`、`千行代码成本（美元/千行）`），不自造似名。

## 说明

- 纯离线、零第三方依赖不变；综合效率分公式核心不变。
- 全套 479 passed, 1 skipped。

---

**完整变更**：`v1.1.1...v1.2.0`