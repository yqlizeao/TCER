# TCER v1.0.13

本次对指标分类里的全部指标做了一轮「计算与语义一致性」核查：用每个指标声明的公式独立重算并与流水线输出逐一比对，覆盖 Claude / Codex / Grok / omp 真实数据。结论是绝大多数指标计算正确、与语义相符；核查中发现并修复了两处不符，并顺势把搜索路径做成一个有价值的展示。

## 修复

### 「涉及文件」不再把搜索目录当文件
「涉及文件」的语义是「独立**文件**数」，但此前统计了所有带路径的工具调用：

- Claude 的 Grep/Glob 不带路径，天然不受影响；但 **Grok / omp 的 Grep 会带路径，且常是目录**（如 `.../tcer/core`）。
- 实测某 Grok 项目「涉及文件」58 个里混入了 7 个目录，虚高约 12%，还污染了弹窗的文件列表。
- 现在只统计真正读 / 写 / 改过的文件；仅被搜索碰过的路径不计入（若同一路径也被读写则保留）。

### 「高返工文件」「一次写对率」可入图
这两个指标在网格里有正常数值，但因内部属性名与键不一致，图表取值返回空——选它们作趋势 / 散点 / 雷达轴时画不出点。现已修正，网格与图表口径一致。

## 新功能

### 涉及文件弹窗新增「搜索足迹」
上面把搜索目录从「涉及文件」剔除后，那些搜索范围本身是有价值的探索信号，不应丢弃——现单独成块展示：

- 涉及文件子窗口现在三块结构清晰：**文件列表**（真实读/写/改的文件）、**目录热度**（文件所在模块热度）、**搜索足迹**（Grep/Glob 扫过的路径及次数）。
- 「搜索足迹」明确标注「不计入涉及文件」，含目录与文件、按搜索次数排序，反映 AI 的探索范围。
- Claude 会话（Grep 无路径）不显示该区块，不产生空块。

## 说明

- 上述修复不改动 TCER / CTEI / 成本等核心计算口径，闭环审计口径不变（41/41 通过）。
- 新增回归护栏：搜索路径独立收集、涉及文件排除搜索目录、弹窗三块渲染、数值指标须可入图。

---

**完整变更**：`v1.0.12...v1.0.13`