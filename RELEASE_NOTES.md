# TCER v1.0.14

本次以「指标计量正确性」为主线：修复了两处**用户可见**的计量缺陷（综合评分保存基准后不刷新、部分指标图表画不出点），并把 omp / Pi / Grok 已落盘却未采集的几个信号接线到既有指标，使它们从「不适用」变为真实数据。核心 TCER / CTEI / 成本口径不变，闭环审计 41/41 通过。

## 修复

### 综合评分（CTEI）保存个人基准后不再刷新
「保存个人基准」后，CTEI 计算仍沿用**启动时**的旧基准，直到重启程序才生效——期间评分与评级可能有近 2× 偏差。

- 根因：`ctei()` / `compute()` 用默认参数固定了基准值，而 Python 默认参数在函数定义时就冻结，`保存基准` 重绑的全局值进不去。
- 现改为调用时读取实时基准：保存后**重新分析即时生效**，无需重启、无需重装。

### 部分指标选作图表轴时画不出点
窗口使用率、首字延迟、首字延迟 P95、推理输出占比、任务完成率、补丁成功率——这些指标在网格里显示正常数值，但选作趋势 / 散点 / 雷达 / 仪表板的轴时是空白。

- 根因：图表取值走的映射表比网格显示的窄，凡「属性名与指标键不一致」的指标取值落空。
- 现让图表取值与网格显示共用同一张映射（SSOT），「网格有数值 ⟺ 图表可绘」永久成立；并加了遍历式回归护栏锁死整类问题。

## 新增数据接入

把以下已在会话日志里、但此前未被采集的信号接入既有指标（对应指标从「不适用」变真实数据）：

- **推理档位**（omp / Pi）：从 `thinking_level_change` 提取，进入「推理档位」及决策实验室对比维度。
- **中断任务**（omp / Pi）：从 `stopReason == "aborted"` 提取（语义同 Codex 的 `turn_aborted`），含中断原因分布。
- **图片输入**（omp / Pi）：统计用户消息里的内联图片块。
- **上下文压缩**（Grok）：从 `signals.json` 的 `compactionCount` 接入。

## 其它

- 决策实验室「Agent 工具」维度补齐为全部六个数据源（此前标签漏了 omp / Pi）。
- 闭环审计新增一致性护栏：「中断原因分布之和」必须等于「中断任务数」。

## 说明

- 上述改动不改动 TCER / CTEI / 成本等核心计算口径；闭环审计口径不变（41/41 通过）。
- 已保存过个人基准的用户：升级后重新分析一次即可看到修正后的评分。

---

**完整变更**：`v1.0.13...v1.0.14`