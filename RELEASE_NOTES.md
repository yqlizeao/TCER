# TCER v1.3.0

洞察与意见面板深度进化 + 效率榜双视角重做。对标 Claude Code `/insights`，在纯离线、零 LLM 的确定性引擎内把「洞察」从阈值告警升级为带具体证据、可执行建议的诊断面板。

## 洞察与意见

- **证据具体化**：从「返工率 46%」升级为「…改得最多的是 views.py（3 次）」「…出错最多的是 Bash（8 次）」——用 `high_churn_details` / `tool_errors_by_tool` 指名到文件/工具。
- **项目级工具失败归纳**：跨会话聚合，产出「工具失败集中在 Bash：106 次里 56 次出在 Bash（53%）」+ 可执行建议（对标 /insights 的 Command Failed 栏）。
- **活动概览**：确定性会话画像——任务类型、工具 top、活跃时段、会话规模、净增行/工具调用总量（对标 What You Work On 的可量化部分）。
- **可复制 CLAUDE.md 规则**：系统性短板 → 结构化规则块（`## 代码修改纪律` + 具体条目），带复制按钮，可直接粘进 CLAUDE.md / AGENTS.md。
- **值得一试 / 进阶工作流（前瞻）**：针对检测到的摩擦，给可粘贴的自然语言 prompt（改完即测、测试先行、自主修复循环、并行子代理拆解等）。
- **严重度分级话术**：返工/工具错误按程度给不同措辞（有一定返工 / 返工偏多 / 返工严重），减少呆板；新增金额类等更多规则。
- 指标名/单位全对齐指标 SSOT（`TCER（行/百万）`、`千行代码成本（美元/千行）`）。

## 效率榜（双视角）

- **移除贡献归因榜**：两视角统一名次榜列集（#/标题/效率分/等级）。
- **项目视角**：隐藏会话排名表与评级分布（在项目视角无意义），右侧项目画像独占全宽——项目概览 → 得分构成 → 洞察 → CLAUDE.md 建议 → 值得一试 → 前瞻 → 活动概览。
- **会话视角**：显示排名表并收窄，仅作会话定位导航。
- **视角切换单一入口**：仅左上角分段控件切换；点排名行只选中、不翻转视角。

## 修复

- `CollapsibleSection` 折叠错位（展开后正文跑到末尾）——重构为单容器 + `after=header` 锚定，对齐指标分类页的正确折叠行为。
- 得分构成进度条被压扁——`pack_propagate(False)` + 固定高度。
- 部分文字过小（硬编码 7pt）——改用主题 `FONT_UI_SMALL`。
- 移除面板底部的引导提示文字。

## 个人基准（承接本轮）

- 校准弹窗重做：先开窗后校准、上下布局、全部会话 / 逐项目双模式、离群过滤默认开的可见开关、中位数/平均数计算方法、基准不受时间范围影响。
- 有效会话阈值 `MIN_BASELINE_SESSIONS` 10 → 5。

## 说明

- 纯离线、零第三方依赖不变；综合效率分公式核心不变。
- 未做的 Claude /insights 维度（主题聚类叙事、工作风格人格画像）需 LLM，与项目定位冲突，刻意不做。
- 全套 492 passed, 1 skipped。

---

**完整变更**：`v1.2.0...v1.3.0`